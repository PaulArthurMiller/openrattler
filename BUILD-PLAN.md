# Build Plan: Universal Action Approval Framework

Generated by /plan on 2026-04-03  
This plan covers **Build Pieces 39.1–39.5**.  
Prior pieces (0.1–38.4) are complete and merged to main — see PROGRESS.md for history.

---

## What We're Building

An action approval gate that sits between an agent's *intent to act* and *execution*. Currently the system has a binary `requires_approval: bool` flag on each tool. This plan replaces it with a five-level severity spectrum, adds non-bypassable dead stops, integrates with the existing security profiles, time-bounds approval tokens against replay, and routes approval requests to the right channel (CLI, Slack, or email) with rationale displayed independently of the action description.

---

## System Architecture

```
  Agent wants to call a tool
           │
           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  ToolExecutor.execute()                                     │
  │                                                             │
  │  1. Look up ToolDefinition → get action_level (1–5)        │
  │  2. Check DEAD_STOP_ACTIONS ──► DeadStopError (never runs) │
  │  3. Check ApprovalThresholdPolicy.requires_approval(level) │
  │     │                                                       │
  │     ├─ No  ──► Execute directly                            │
  │     │                                                       │
  │     └─ Yes ──► build ApprovalRequest (with level,          │
  │                 rationale, trace_id, token)                 │
  │                 │                                           │
  │                 ▼                                           │
  │          ApprovalManager.request_approval()                 │
  │                 │                                           │
  │          fires handler (background task)                    │
  │          awaits asyncio.Event with timeout                  │
  │                 │                                           │
  │          ┌──────┴──────────────────┐                       │
  │          │ ApprovalHandlerRouter   │                       │
  │          │                         │                       │
  │          │  CLI ─► CLIApproval     │                       │
  │          │  Slack ─► SlackApproval │                       │
  │          │  Email ─► EmailApproval │                       │
  │          └──────────────────────── ┘                       │
  │                 │                                           │
  │     ┌───────────┴──────────┐                               │
  │     │                      │                               │
  │   Approved             Denied / Timeout                    │
  │     │                      │                               │
  │  Verify token          Block execution                     │
  │  not expired               │                               │
  │     │                 Audit log denial                     │
  │  Execute tool              │                               │
  │  Audit log approval        ▼                               │
  │  (single-use token      Return ToolResult(error)           │
  │   consumed)                                                 │
  └─────────────────────────────────────────────────────────────┘
```

### Why the dead stop check is separate from the threshold

The threshold is a policy dial — users can tune it, and a compromised config could theoretically lower it. Dead stops are code — a `frozenset[str]` compiled into the binary that no config key can override. The dead stop check runs before the policy check so there is no code path that bypasses it. This is the same design principle as `AUTHORIZED_SPAWNERS` in `creator_validator.py`.

### Why rationale is displayed independently

An agent could craft a rationale that visually overshadows the action description ("Just tidying up some files — safe to approve" when the action is `delete_workspace`). The handler templates print **Action** and **Rationale** in separate, clearly-labeled blocks, in that order. The action always comes first, so the user reads *what* before they read *why*.

---

## Data Flow: Approval Request Lifecycle

```
ToolExecutor                ApprovalManager            Handler (CLI/Slack/Email)
─────────────               ───────────────            ─────────────────────────
build ApprovalRequest
  action_level = 2
  rationale = "agent rationale"
  trace_id = UM trace_id
  token = uuid4()
  expiry = now + 5min
        │
        ▼
request_approval(req) ──►  store pending
                           audit: approval_requested
                           create_task(handler(req))  ──►  format message
                           await event (timeout=30s)        [ACTION] Push to remote
                                                            [LEVEL]  2 — significant,
                                                                     hard to reverse
                                                            [REASON] agent rationale
                                                            Approve? [y/N]
                                                                   │
                                                           resolve(id, approved=True)
                                                                   │
                           ◄──────────────────────────────────────┘
                           store result
                           audit: approval_resolved
                           event.set()
        │
        ◄── ApprovalResult(approved=True, token=..., expiry=...)
        │
verify token not expired
execute tool
audit: tool_executed (token consumed)
```

---

## Key Files Affected

```
openrattler/
├── models/
│   └── tools.py              ← add action_level: int = 5 to ToolDefinition
├── security/
│   ├── action_levels.py      ← NEW: ACTION_LEVEL_DESCRIPTIONS, DEAD_STOP_ACTIONS,
│   │                              DeadStopError, ApprovalThresholdPolicy
│   └── approval.py           ← enhance ApprovalRequest (level, rationale, trace_id, token),
│                               add token expiry tracking, dead stop check,
│                               update CLIApprovalHandler (level + rationale display),
│                               add ApprovalHandlerRouter
├── config/
│   └── loader.py             ← add approval_threshold: Optional[int] to SecurityConfig
├── tools/
│   ├── permissions.py        ← update needs_approval() to accept policy
│   ├── executor.py           ← dead stop check, level lookup, token expiry verify,
│   │                           trace_id propagation
│   └── builtin/
│       ├── channel_tools.py  ← add action_level to each tool definition
│       ├── file_ops.py       ← add action_level to each tool definition
│       ├── session_tools.py  ← add action_level to each tool definition
│       └── memory_tools.py   ← add action_level to each tool definition
├── channels/
│   ├── slack_adapter.py      ← add SlackApprovalHandler
│   └── email_adapter.py      ← add EmailApprovalHandler
└── startup.py                ← create policy + router, wire into ToolExecutor
```

---

## Build Order

The pieces are ordered so that each one compiles and passes tests independently before the next one builds on it.

---

### Piece 39.1 — Action Level Model + Dead Stop Registry

**Build:**
- Create `openrattler/security/action_levels.py` with:
  - `ACTION_LEVEL_DESCRIPTIONS: dict[int, str]` — human-readable label for each level (e.g. `{1: "Permanent, irreversible, or financial cost", ...}`)
  - `DEAD_STOP_ACTIONS: frozenset[str]` — hardcoded set of action names that may never execute (e.g. `"force_push_main"`, `"drop_table"`, `"rm_workspace_recursive"`). This is a frozenset in code, not a config key.
  - `DeadStopError(action: str)` exception — raised when a dead-stop action is attempted; carries the action name for audit logging
- Add `action_level: int = 5` to `ToolDefinition` in `openrattler/models/tools.py`
  - Default is 5 (read-only/sandboxed) — safest possible default; tools must opt into lower levels explicitly
  - Keep `requires_approval: bool` as-is for backward compat for now
- Annotate every existing built-in tool in `tools/builtin/` with its correct level:
  - `channel_tools.py`: Slack send = level 2, email send = level 1
  - `file_ops.py`: file write = level 3, file delete = level 1
  - `session_tools.py`: session create = level 4
  - `memory_tools.py`: memory read = level 5, memory write = level 3, `update_heartbeat` = level 3
- Tests in `tests/test_security/test_action_levels.py`:
  - Dead stop frozenset is immutable
  - All ACTION_LEVEL_DESCRIPTIONS keys are 1–5
  - Every built-in tool has an action_level in range 1–5
  - DeadStopError carries the action name

**Why first:** The level field and registry are the foundation. Every downstream piece references them. If the level concept turns out to need adjustment (e.g. the default value, the enum shape), better to catch that before the policy and executor code depend on it.

**Done when:** `pytest`, `mypy`, `black` all pass. Every existing built-in tool has an explicit `action_level`.

---

### Piece 39.2 — Enhanced ApprovalRequest + Token System

**Build:**
- In `openrattler/security/approval.py`, update `ApprovalRequest`:
  - Add `action_level: int` — declared by executor, not agent
  - Add `rationale: Optional[str] = None` — agent's stated reason (displayed independently)
  - Add `trace_id: Optional[str] = None` — UM trace for cross-system correlation
  - Add `approval_token: str` — UUID generated by the executor; single-use
  - Add `token_expiry: datetime` — `now + token_lifetime_seconds` (default 5 min)
- In `ApprovalResult`, add `approval_token: str` and `token_expiry: datetime` (passed through from the request for the executor to verify)
- In `ApprovalManager`:
  - Add `_consumed_tokens: set[str]` — tokens that have already been used for execution
  - Add `consume_token(token: str, expiry: datetime) -> bool` — validates token is unused and not expired; marks it consumed; returns False (deny) if invalid. This is called by `ToolExecutor` immediately before execution, not at approval time.
  - Update `request_approval` audit event to include `action_level`, `trace_id`
  - Update `resolve` audit event to include `action_level`
- Add dead stop check to `ApprovalManager.request_approval()`:
  - If `request.operation` is in `DEAD_STOP_ACTIONS`, raise `DeadStopError` immediately (no handler is called, no event is set)
  - Log `audit: dead_stop_blocked` with full parameters before raising

**Why this order:** The enhanced request model must exist before the policy (39.3) decides whether to create one, and before the handlers (39.4) format one.

**Done when:** Tests cover token expiry (expired token → False), token replay (consumed token → False), dead stop audit log, and that rationale/trace_id/level flow through to the audit event.

---

### Piece 39.3 — Threshold Policy + Config Integration

**Build:**
- Create `ApprovalThresholdPolicy` in `openrattler/security/action_levels.py`:
  ```
  class ApprovalThresholdPolicy:
      threshold: int  # levels <= threshold require approval
      def requires_approval(self, level: int) -> bool
      @classmethod
      def from_security_config(cls, config: SecurityConfig) -> ApprovalThresholdPolicy
  ```
  - Profile → threshold mapping: `paranoid=5`, `standard=3`, `minimal=1`
  - If `config.approval_threshold` is set, it overrides the profile default
  - `requires_approval(level)` returns `level <= self.threshold`
- Add `approval_threshold: Optional[int] = None` to `SecurityConfig` in `openrattler/config/loader.py`
  - Valid range: 1–5; validated on load
  - None means "use profile default"
- Update `needs_approval()` in `openrattler/tools/permissions.py`:
  - New signature: `needs_approval(tool_def: ToolDefinition, policy: ApprovalThresholdPolicy) -> bool`
  - Returns `policy.requires_approval(tool_def.action_level)`
  - The old `tool_def.requires_approval: bool` field is **not removed yet** — it becomes unused/deprecated and will be cleaned up in a later piece once everything is wired

**Why this order:** The policy object must exist before the executor (39.5) can use it. Config integration is self-contained and doesn't touch the handler or executor code.

**Done when:** Tests cover all three profiles + direct threshold override + boundary cases (level exactly at threshold, level one above).

---

### Piece 39.4 — Handler Upgrades + Multi-Channel Routing

**Build:**
- Update `CLIApprovalHandler._print_request()` in `approval.py`:
  - Display format:
    ```
    ════════════════════════════════════════════════
    [APPROVAL REQUIRED]
      Action  : push_to_remote_branch
      Target  : origin/feat/new-feature
      Level   : 2 — Significant, hard to reverse
      Agent   : agent:main
      Session : agent:main:abc123
      Timeout : 30s
    ────────────────────────────────────────────────
      Reason (agent's stated rationale):
      "The feature branch is ready for review and
       tests are passing."
    ════════════════════════════════════════════════
    ```
  - Rationale block always renders AFTER the action block, separated by a divider
  - If `rationale` is None, print `"(no rationale provided)"`
- Add `SlackApprovalHandler` in `openrattler/channels/slack_adapter.py`:
  - Posts a Slack message with the same two-block structure (action block, then rationale block)
  - Listens for reply messages containing `approve` or `deny` (case-insensitive) from an allowlisted sender
  - On match, calls `manager.resolve()`
  - Uses existing `SlackAdapter` infrastructure (bot token, channel ID from config)
- Add `EmailApprovalHandler` in `openrattler/channels/email_adapter.py`:
  - Sends a formatted email to `default_to_address`
  - Subject: `[APPROVAL REQUIRED] {operation} — Level {level}`
  - Body: same two-block structure
  - Polls IMAP for a reply containing `approve` or `deny` in the body, from an allowlisted sender
  - Uses existing `EmailAdapter` infrastructure
- Add `ApprovalHandlerRouter` in `openrattler/security/approval.py`:
  - Constructor takes `preferred_channel: str` and a mapping `{channel_name: handler}`
  - `__call__(request, manager)` dispatches to the right handler
  - Falls back to `CLIApprovalHandler` if preferred channel is unconfigured
  - Always callable (never raises for missing channel config — falls back gracefully)

**Why this order:** Handlers are leaf components — they depend on the enhanced request model (39.2) but nothing else depends on them. They can be built and tested with a mock manager before the executor integration.

**Done when:** Tests cover correct format rendering (level label shown, rationale block separate), fallback to CLI when Slack/Email not configured, and router dispatch logic.

---

### Piece 39.5 — ToolExecutor + Startup Integration

**Build:**
- Update `ToolExecutor.execute()` in `openrattler/tools/executor.py`:
  - Accept `policy: ApprovalThresholdPolicy` as constructor parameter (alongside existing `approval_manager`)
  - **Step 1:** Look up `tool_def.action_level`
  - **Step 2:** Check `DEAD_STOP_ACTIONS` — if hit, log `dead_stop_blocked` audit event and return `ToolResult(error=DeadStopError message)`. Do not raise (executor contract returns ToolResult, not exceptions).
  - **Step 3:** Check `needs_approval(tool_def, policy)` — if False, execute directly (no change from current flow for below-threshold actions)
  - **Step 4:** If approval needed, build `ApprovalRequest` including:
    - `action_level` from tool def
    - `rationale` from `tool_call.rationale` if present (new optional field on ToolCall, or from `tool_call.context`)
    - `trace_id` from `tool_call.trace_id` if present
    - Fresh `approval_token = uuid4()`
    - `token_expiry = now + token_lifetime_seconds`
  - **Step 5:** `await approval_manager.request_approval(request)`
  - **Step 6:** If approved, call `approval_manager.consume_token(result.approval_token, result.token_expiry)` — if this returns False (expired or replayed), log audit event and return `ToolResult(error="Approval token expired or replayed")`
  - **Step 7:** Execute tool
- Update `openrattler/startup.py`:
  - Build `ApprovalThresholdPolicy.from_security_config(config.security)` at startup
  - Build `ApprovalHandlerRouter` based on `config.channels` (what's configured determines what's available)
  - Wire both into `ToolExecutor`
- Backward-compat note: The old `requires_approval: bool` field on `ToolDefinition` can now be formally deprecated (add a comment; removal deferred to avoid breaking any external callers that might reference it)

**Why last:** This piece closes the loop — it wires together all the components built in 39.1–39.4 and validates the end-to-end flow. Integration bugs (type mismatches, wrong wiring) show up here.

**Done when:** End-to-end integration test covers: (1) level-5 tool executes without approval prompt, (2) level-2 tool triggers approval gate, (3) dead-stop tool returns error without prompting, (4) expired token is rejected, (5) denied approval blocks execution. All existing 211+ tests still pass.

---

## Edge Cases & Open Questions

**39.1:**
- ? Which actions should be in `DEAD_STOP_ACTIONS` on day one? Start conservative: `force_push_main`, `drop_table`, `rm_workspace_recursive`. Add more as the tool set grows.
- ⚠ MCP tools injected at runtime don't have static `action_level` annotations — `mcp/manager.py` currently forces them to `requires_approval=True`. In 39.5, treat MCP tools with no `action_level` as level 1 (strictest safe default).

**39.2:**
- ⚠ The `_consumed_tokens` set grows unboundedly in a long-running process. Add a periodic cleanup that removes entries where `expiry < now - grace_period`. This can be a simple check at the top of `consume_token()`.
- ? `rationale` field on `ToolCall` — does it exist? Check `openrattler/models/messages.py`. If not, add it as `Optional[str] = None`.

**39.3:**
- ? Config validation: if `approval_threshold` is set to 6 or 0, should `load_config` error or clamp? Decision: error with a clear message (silent clamping hides misconfiguration).

**39.4:**
- ⚠ Slack polling for approval reply adds a loop to an existing polling system. Keep the approval poll timeout short (equal to `request.timeout_seconds`) and ensure it cancels cleanly when `manager.resolve()` is called from another path (e.g. simultaneous CLI resolve).
- ⚠ Email approval polling is inherently slow (IMAP polling interval). The default `timeout_seconds` of 30 may be too short for email; consider a per-channel timeout override or a longer default for email delivery.

**39.5:**
- ? `ToolCall` may not have a `trace_id` field today. If not, add `trace_id: Optional[str] = None` to `ToolCall` and propagate from `UniversalMessage.trace_id` at the point where `ToolCall` objects are created.
- ⚠ The old `requires_approval: bool` on `ToolDefinition` will now be ignored by the executor. Add a deprecation comment in the model. Don't remove it yet — MCP bridge references it and removal needs its own PR.

---

## Conventions for This Build

- `DEAD_STOP_ACTIONS` uses the same frozenset pattern as `AUTHORIZED_SPAWNERS` — compile-time constant, never read from config
- Action levels are `int`, not an `Enum`, to keep JSON serialization trivial and avoid import cycles
- The `approval_token` UUID is generated by `ToolExecutor`, not by the agent or the `ApprovalManager` — the executor owns the execution gate
- Audit events for this feature use event names: `approval_requested`, `approval_resolved`, `dead_stop_blocked`, `approval_token_rejected`
- All new handler classes follow the existing `ApprovalHandler` callable protocol: `async (request, manager) -> None`

---

## Status

Current milestone: **complete** — all 39.x pieces done  
Piece 39.1: ✅ complete — PR #54  
Piece 39.2: ✅ complete — PR open  
Piece 39.3: ✅ complete — PR open  
Piece 39.4: ✅ complete — PR open  
Piece 39.5: ✅ complete — PR open  
Last updated: 2026-04-03
---

---

# Build Plan: Claude Code Integration (40.x)

Generated by /planner on 2026-04-17
Follows: Build Pieces 39.1–39.5 (Universal Action Approval Framework — complete)

---

## What We're Building

A capability for the personal assistant to invoke Claude Code
(CC) as a subprocess and use it for software development tasks — writing code, running
tests, making commits — while keeping CC's work structurally isolated from the human
user's files, repos, and personal Claude config.

The integration uses **Option A** (task-level gating): the assistant approves the task
description before spawning CC, then CC runs non-interactively within a sandboxed
workspace. A **plan review step** (adapted from Option B) runs CC in read-only mode
first to produce a plan, which becomes its own approval request before execution
begins.

---

## System Architecture

```
┌────────────────────────────────────────────────────────────────────────┐
│  OPENRATTLER — existing system                                         │
│                                                                        │
│  Channel (Slack/Email/CLI)                                             │
│       │                                                                │
│       ▼                                                                │
│  Runtime → ToolExecutor.execute("cc_write_task", {...})                │
│                 │                                                      │
│         [Dead stop check]  ←── DEAD_STOP_ACTIONS (compile-time)       │
│         cc_bypass_permissions? → hard stop, no prompt                 │
│         cc_push_main?          → hard stop, no prompt                 │
│                 │                                                      │
│         [Threshold check]                                              │
│         ApprovalThresholdPolicy → requires_approval(level)?           │
│                 │                                                      │
│         [Approval gate — existing 39.x system]                        │
│         CLI / Slack / Email → human approves task description         │
│                 │                                                      │
│         ClaudeCodeTool.execute(task, project, scope)                  │
└────────────────────┬───────────────────────────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────────────────────────────┐
│  CLAUDE CODE INTEGRATION LAYER (new — 40.x)                           │
│                                                                        │
│  WorkspaceManager                                                      │
│  ├── validate_path(path) → ensures within assistant workspace         │
│  ├── create_project("task-name") → {prefix}-task-name/                │
│  └── write_claude_md(project) → soft structural fence in each repo    │
│                                                                        │
│  ─────── PLAN PHASE (Option B adaptation) ──────────────────          │
│  CCSubprocessManager.spawn_readonly(task, project)                    │
│  └── claude -p "{task}"                                               │
│          --output-format json                                          │
│          --allowedTools "Read,Glob,Grep,LS"    ← read-only            │
│          --add-dir "{workspace}"               ← path fence           │
│          env: CLAUDE_CONFIG_DIR={workspace}\.claude  ← isolated cfg   │
│                                                                        │
│  CCPlanReviewer → formats plan as approval request (level 3)          │
│  → existing ApprovalManager → user approves plan                      │
│                                                                        │
│  ─────── EXECUTION PHASE ───────────────────────────────              │
│  CCSubprocessManager.spawn_exec(task, project)                        │
│  └── claude -p "{task}"                                               │
│          --output-format json                                          │
│          --allowedTools "{configured_exec_tools}"                     │
│          --add-dir "{workspace}"               ← path fence           │
│          env: CLAUDE_CONFIG_DIR={workspace}\.claude  ← isolated cfg   │
│                                                                        │
│  CCOutputParser → UniversalMessage(type="cc_result")                  │
└────────────────────┬───────────────────────────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────────────────────────────┐
│  ASSISTANT WORKSPACE  C:\Users\{assistant_name}\                      │
│  (default: C:\Users\OpenRattler\ — configured via CCConfig)           │
│                                                                        │
│  .claude\                     ← isolated CC config (not human user's) │
│  projects\                                                             │
│  ├── {prefix}-auth-fix\       ← git repo, {prefix}/ branches only    │
│  │   ├── .git\                                                         │
│  │   │   └── config           ← author = {git_author_name}            │
│  │   ├── CLAUDE.md            ← soft fence: per-repo CC rules         │
│  │   └── [CC's work...]                                                │
│  └── {prefix}-new-feature\                                             │
│                                                                        │
│  STRUCTURAL FENCES (in order of hardness):                            │
│  ① --add-dir enforces CC stays in workspace (CC-level, hard)          │
│  ② CLAUDE_CONFIG_DIR isolates CC config from user's (env, hard)       │
│  ③ git pre-push hook blocks push to main branch (git-level, hard)     │
│  ④ DEAD_STOP_ACTIONS blocks cc_bypass/cc_push_main (code, hard)       │
│  ⑤ CLAUDE.md in each project instructs CC on constraints (soft)       │
└────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: A Complete CC Task

```
USER (Slack): "Fix the login bug in the auth module"
       │
       ▼
The assistant formulates task description (with full conversation context)
       │
       ▼
[APPROVAL GATE — Level 3]
  Action  : cc_write_task
  Target  : {prefix}-auth-fix (new project in workspace)
  Level   : 3 — Moderate, modifies local state
  Reason  : "Fix login bug in auth module"
  Approve? [y/N]     ← human approves via configured channel
       │
       ▼
WorkspaceManager.create_project("auth-fix")
→ C:\Users\{assistant_name}\projects\{prefix}-auth-fix\
→ writes CLAUDE.md with workspace rules
→ inits git repo, sets assistant author identity, installs pre-push hook
       │
       ▼
[PLAN PHASE — read-only CC spawn]
CC: --allowedTools Read,Glob,Grep,LS
CC analyzes codebase, produces plan output (JSON)
CCPlanReviewer formats:
  "I will patch SessionValidator.verify() in auth/validator.py
   and add a regression test in tests/test_auth.py"
       │
       ▼
[PLAN APPROVAL — Level 3]
  Plan    : Patch SessionValidator.verify() + add test
  Files   : auth/validator.py, tests/test_auth.py
  Approve? [y/N]     ← auto-approved under minimal profile
       │
       ▼
[EXECUTION PHASE — full CC spawn]
CC: --allowedTools Read,Write,Bash,Glob,Grep
CC implements, tests, commits to branch {prefix}/auth-fix
Output captured as JSON → CCOutputParser
       │
       ▼
UniversalMessage(type="cc_result", content=summary, metadata={branch, commit, tokens})
→ Assistant sends to Slack: "Done. Branch {prefix}/auth-fix is ready for your review."
```

The human user controls what happens next — reviewing the branch and merging
(or not) is entirely outside the assistant's reach.

---

## Key Files

```
openrattler/
├── tools/
│   └── claude_code/               ← NEW module
│       ├── __init__.py
│       ├── tool.py                ← ClaudeCodeTool: registered tool, full orchestration
│       ├── subprocess_mgr.py      ← CCSubprocessManager: spawn, timeout, kill, limits
│       ├── plan_reviewer.py       ← CCPlanReviewer: read-only phase, plan formatting
│       ├── output_parser.py       ← CC JSON output → ToolResult / UniversalMessage
│       └── workspace.py           ← WorkspaceManager: path enforcement, git identity
├── config/
│   └── loader.py                  ← add CCConfig section
├── security/
│   └── action_levels.py           ← extend DEAD_STOP_ACTIONS with cc_* entries
└── templates/
    └── cc_workspace_claude_md.txt ← CLAUDE.md template for workspace projects (soft fence)
```

---

## CC-Specific Tool Definitions and Action Levels

These are the tool names exposed to the agent runtime. Action levels follow the
same 1–5 scale as all other tools.

| Tool name            | Level | Description                                          |
|----------------------|-------|------------------------------------------------------|
| `cc_read_analyze`    | 5     | Read-only CC session: code review, explanation, Q&A  |
| `cc_write_task`      | 3     | CC writes/modifies files within workspace            |
| `cc_run_with_shell`  | 2     | CC task that includes shell command execution (Bash) |
| `cc_git_commit`      | 3     | CC commits staged changes to an assistant branch     |
| `cc_git_push_branch` | 2     | CC pushes an assistant branch to remote              |
| `cc_promote_request` | 1     | Request to merge an assistant branch (always level 1)|

Dead stops — added to `DEAD_STOP_ACTIONS` (compile-time frozenset, no config override):

| Action name               | Reason                                             |
|---------------------------|----------------------------------------------------|
| `cc_bypass_permissions`   | Never pass --dangerously-skip-permissions to CC    |
| `cc_push_main`            | CC may never push directly to main                 |

Note: Path-outside-workspace violations are caught structurally by
`--add-dir` (CC-level) and `WorkspaceManager.validate_path()` (tool-level).
They do not need a named dead stop — the path fence prevents them at the
subprocess boundary.

---

## Build Order

---

### Piece 40.1 — Config + Workspace Foundation

**Build:**
- Add `CCConfig` to `openrattler/config/loader.py`:
  ```python
  class CCConfig(BaseModel):
      enabled: bool = False
      workspace_root: str          # absolute path; default C:\Users\{assistant_name}\
      assistant_name: str          # mirrors top-level name; used for default workspace path
      project_prefix: str          # default: first component of assistant_name lowercased ("or")
      git_author_name: str         # e.g. "[assistant_name] (OpenRattler)"
      git_author_email: str        # e.g. "[assistant_name]@openrattler.local"
      max_concurrent_sessions: int = 1
      session_timeout_seconds: int = 300
      plan_review_enabled: bool = True
      plan_review_approval_level: int = 3   # level used for plan approval request
      exec_allowed_tools: list[str] = ["Read", "Write", "Glob", "Grep", "LS", "Bash"]
      claude_binary: str = "claude"  # full path if not in PATH
  ```
- Create `openrattler/tools/claude_code/workspace.py` — `WorkspaceManager`:
  - `ensure_workspace()` — creates `workspace_root\projects\` and `workspace_root\.claude\` if absent
  - `create_project(name: str) -> Path` — creates `{workspace_root}\projects\{prefix}-{name}\`,
    inits git repo, writes local git config (author name + email), installs pre-push hook
    (hook script: `if git rev-parse --abbrev-ref HEAD == "main": exit 1`)
  - `write_project_claude_md(project_path: Path)` — renders template to `{project}\CLAUDE.md`
  - `validate_path(path: Path) -> bool` — returns True only if path is within workspace_root
  - `get_env(project_path: Path) -> dict[str, str]` — returns env vars to pass to CC subprocess,
    including `CLAUDE_CONFIG_DIR={workspace_root}\.claude`
- Create `openrattler/templates/cc_workspace_claude_md.txt` — CLAUDE.md template with:
  - Workspace path constraint (never write files outside CWD)
  - Branch convention (`{prefix}/` prefix on all git branches)
  - Git author identity reminder
  - Never push to main
  - Never pass --dangerously-skip-permissions in any shell command
- Tests in `tests/test_tools/test_cc_workspace.py`:
  - Workspace creation idempotent (second call doesn't error)
  - `create_project` produces correct path with prefix
  - `validate_path` blocks paths outside workspace_root
  - Pre-push hook is written and is executable (or correct text on Windows)
  - `get_env` returns `CLAUDE_CONFIG_DIR` pointing to workspace `.claude\`

**Why first:** Everything else depends on knowing where CC will work and how projects
are structured. Workspace logic is pure file I/O — no subprocess, no approval system —
so it's the safest first piece and easy to test without external dependencies.

**Done when:** `pytest`, `mypy`, `black` pass. Workspace directory structure created
correctly. Pre-push hook installed. CLAUDE.md template renders with correct prefix and
author identity substitutions.

---

### Piece 40.2 — CC Tool Definitions + Dead Stop Extensions

**Build:**
- Extend `DEAD_STOP_ACTIONS` in `openrattler/security/action_levels.py`:
  ```python
  DEAD_STOP_ACTIONS: frozenset[str] = frozenset({
      # existing
      "force_push_main", "drop_table", "rm_workspace_recursive",
      # new — CC-specific
      "cc_bypass_permissions",
      "cc_push_main",
  })
  ```
- Create `openrattler/tools/claude_code/tool.py` (definitions only in this piece):
  - Define `ToolDefinition` instances for all six cc_* tools (see table above)
  - Each definition includes: `name`, `description`, `action_level`, `parameters` schema
  - `cc_promote_request` parameters: `project_name: str`, `branch: str`, `target: str = "main"`
  - `cc_bypass_permissions` is defined as a dead-stop sentinel (action_level=1, in
    DEAD_STOP_ACTIONS; if an agent ever tries to call it, the dead-stop check fires before
    any handler runs)
- Update `openrattler/tools/registry.py`:
  - Register cc_* tools only when `CCConfig.enabled = True` (gate at startup, not at call time)
  - Import from `tools.claude_code.tool` guarded by config check in `startup.py`
- Tests in `tests/test_tools/test_cc_tool_definitions.py`:
  - All six cc_* tools registered when enabled
  - No cc_* tools registered when disabled
  - `cc_bypass_permissions` in `DEAD_STOP_ACTIONS`
  - `cc_push_main` in `DEAD_STOP_ACTIONS`
  - All cc_* action_levels in valid range 1–5
  - `cc_promote_request` is level 1 (always requires human approval)

**Why this order:** Tool definitions are pure data — no subprocess, no I/O. Registering
them (and their dead stops) before building the executor that runs them means tests in
40.3+ can rely on the registered tool shapes. Same ordering principle as 39.1 in the
approval framework.

**Done when:** `pytest`, `mypy`, `black` pass. Dead stop check blocks `cc_bypass_permissions`
via existing ToolExecutor dead-stop path (no new executor code needed — the existing
39.5 executor handles it).

---

### Piece 40.3 — CCSubprocessManager

**Build:**
- Create `openrattler/tools/claude_code/subprocess_mgr.py` — `CCSubprocessManager`:
  - Constructor takes `CCConfig`
  - `_active_sessions: dict[str, asyncio.subprocess.Process]` — currently running sessions
  - `_validate_args(args: list[str]) -> None` — scans for forbidden flags
    (`--dangerously-skip-permissions`, `--no-permissions`, direct `--permission-mode bypass`);
    raises `DeadStopError("cc_bypass_permissions")` if found — this is the subprocess-level
    structural check (defense in depth alongside the tool-level dead stop)
  - `spawn(task: str, project_path: Path, allowed_tools: list[str], env: dict) -> CCResult`:
    - Checks `len(_active_sessions) < max_concurrent_sessions`; raises if at limit
    - Builds argv: `[claude_binary, "-p", task, "--output-format", "json",
                    "--allowedTools", ",".join(allowed_tools), "--add-dir", str(project_path)]`
    - Calls `_validate_args(argv)` before spawning
    - Spawns with `asyncio.create_subprocess_exec`, `cwd=project_path`, merged env
    - Awaits stdout with `asyncio.wait_for(timeout=session_timeout_seconds)`
    - On timeout: kills process, removes from `_active_sessions`, returns `CCResult(error="timeout")`
    - On success: returns `CCResult(stdout=..., returncode=...)`
  - `kill_all()` — kills all active sessions (used in shutdown)
- Create `openrattler/tools/claude_code/output_parser.py` — `CCOutputParser`:
  - `parse(raw: str) -> CCParsedOutput` — parses `--output-format json` response
  - Fields extracted: `result` (text), `cost_usd`, `num_turns`, `session_id`, `error`
  - Resilient to missing fields (all optional except `result`/`error`)
- Tests in `tests/test_tools/test_cc_subprocess.py`:
  - `_validate_args` raises `DeadStopError` on `--dangerously-skip-permissions`
  - `_validate_args` raises `DeadStopError` on `--permission-mode bypass`
  - Session limit: third spawn when limit=2 raises immediately
  - Timeout: mock slow process → CCResult with error="timeout"
  - `kill_all` terminates all tracked processes
  - `CCOutputParser.parse` handles missing `cost_usd` gracefully
  - `CCOutputParser.parse` returns error field when CC exits non-zero

**Note on Windows process kill:** `asyncio.subprocess.Process.kill()` calls
`TerminateProcess` on Windows (equivalent to `SIGKILL`), which is the correct
behavior. No platform-specific handling needed here.

**Why this order:** The subprocess manager is the lowest-level component — it has no
dependency on the plan reviewer or the full tool orchestration. Building and testing it
in isolation means the most complex external-process logic is validated before it's
wrapped in the higher-level flow.

**Done when:** `pytest`, `mypy`, `black` pass. All subprocess tests pass with the CC
process mocked at the `asyncio.create_subprocess_exec` boundary. No real CC invocation
required.

---

### Piece 40.4 — Plan Review Approval Step

**Build:**
- Create `openrattler/tools/claude_code/plan_reviewer.py` — `CCPlanReviewer`:
  - Constructor takes `CCSubprocessManager`, `ApprovalManager`, `CCConfig`,
    `ApprovalThresholdPolicy`
  - `review(task: str, project_path: Path, env: dict) -> PlanReviewResult`:
    - Spawns CC read-only: `allowed_tools = ["Read", "Glob", "Grep", "LS"]`
    - Task prompt is prefixed with: *"Analyze the codebase and describe the plan you
      would follow to complete the following task. Do not make any changes. Output a
      structured plan including: files to be modified, files to be created, shell
      commands to be run (if any), and reasoning. Task: {task}"*
    - Parses CC's JSON output → extracts plan text
    - If `not config.plan_review_enabled`: returns `PlanReviewResult(approved=True, plan=None)`
    - Builds `ApprovalRequest`:
      - `operation = "cc_plan_review"`
      - `action_level = config.plan_review_approval_level` (default 3)
      - `rationale = "Plan for: {task}"`
      - Displays plan text as the action description in the approval prompt
    - Calls `approval_manager.request_approval(request)` — uses existing 39.x system
    - Returns `PlanReviewResult(approved=bool, plan=str)`
- Tests in `tests/test_tools/test_cc_plan_reviewer.py`:
  - Plan review disabled → returns approved=True without calling approval_manager
  - Plan review enabled, approval granted → returns approved=True with plan text
  - Plan review enabled, approval denied → returns approved=False
  - Read-only tool list is passed to subprocess_mgr (assert on spawn call args)
  - Plan text appears in the ApprovalRequest (not just the rationale)

**Why this order:** The plan reviewer composes the subprocess manager (40.3) and the
approval manager (existing 39.x). Both dependencies are stable before this piece starts.

**Done when:** `pytest`, `mypy`, `black` pass. Plan review integrates cleanly with the
existing approval manager — no new approval infrastructure needed, the 39.x system
handles it as a standard approval request.

---

### Piece 40.5 — ClaudeCodeTool Integration + End-to-End

**Build:**
- Complete `openrattler/tools/claude_code/tool.py` — `ClaudeCodeTool`:
  - Implements the callable tool protocol (same as other built-in tools)
  - `execute(tool_call: ToolCall) -> ToolResult`:
    1. Extract `task`, `project_name` from `tool_call.parameters`
    2. `workspace.create_project(project_name)` → `project_path`
    3. `env = workspace.get_env(project_path)`
    4. If plan_review_enabled: `plan_result = await plan_reviewer.review(task, project_path, env)`
       — if `not plan_result.approved`: return `ToolResult(error="Plan not approved")`
    5. `cc_result = await subprocess_mgr.spawn(task, project_path, exec_allowed_tools, env)`
    6. `parsed = output_parser.parse(cc_result.stdout)`
    7. Build and return `ToolResult` wrapping a `UniversalMessage(type="cc_result",
       content=parsed.result, metadata={session_id, cost_usd, num_turns, branch})`
  - `cc_read_analyze` variant: skips plan review, uses read-only tool list, no exec phase
- Update `openrattler/startup.py`:
  - If `config.cc.enabled`:
    - Instantiate `WorkspaceManager(config.cc)`
    - Instantiate `CCSubprocessManager(config.cc)`
    - Instantiate `CCPlanReviewer(subprocess_mgr, approval_manager, config.cc, policy)`
    - Instantiate `ClaudeCodeTool(workspace, subprocess_mgr, plan_reviewer, config.cc)`
    - Register cc_* tools in the tool registry with their definitions
- End-to-end tests in `tests/test_tools/test_cc_integration.py`:
  - Full flow: workspace created → plan approved → CC spawned → result returned
  - Plan denied → ToolResult error, CC spawn never called
  - Dead stop `cc_bypass_permissions` → ToolResult error, no CC spawn, audit log written
  - `cc_promote_request` → level-1 approval triggered (always prompts human)
  - `cc_read_analyze` → no plan review, read-only tool list, result returned
  - CC timeout → ToolResult with error, session cleaned up
  - `CCConfig.enabled = False` → cc_* tools not registered, calls return unknown-tool error

**Why last:** This piece closes the loop — wires WorkspaceManager + SubprocessManager +
PlanReviewer + existing 39.x approval flow into a single callable tool. Integration
bugs surface here.

**Done when:** All existing tests still pass. End-to-end integration tests pass with CC
mocked at the subprocess boundary. `mypy`, `black` pass. Manual smoke test: set
`CCConfig.enabled = True` in local config, invoke `cc_read_analyze` from CLI chat,
confirm CC runs in workspace directory with isolated config.

---

## Edge Cases & Open Questions

**40.1:**
- ⚠ `workspace_root` must be an absolute path. Add validation on config load — error
  clearly if it's relative or doesn't have a drive letter on Windows.
- ? Pre-push hook on Windows: git hooks are shell scripts; Windows git (Git for Windows)
  runs them via bash. Test that the hook fires correctly. If not, document the limitation
  and use a post-commit check as fallback.
- ⚠ If the assistant's Windows user account (`C:\Users\{assistant_name}\`) doesn't exist,
  workspace creation will fail. Document this as a setup prerequisite. Don't silently create it.

**40.2:**
- ? The cc_* tool `parameters` schemas need to be defined. At minimum:
  `cc_write_task`: `{task: str, project_name: str}`;
  `cc_read_analyze`: `{task: str, project_name: str}`;
  `cc_promote_request`: `{project_name: str, branch: str, target: str}`.
  Finalize during 40.2 build.

**40.3:**
- ⚠ `--output-format json` behavior should be verified against the installed CC version
  before 40.3 build begins. Run `claude -p "say hello" --output-format json` manually
  and capture the exact JSON schema. The output_parser must match reality.
- ⚠ `--add-dir` flag behavior: verify this restricts CC to the specified directory on
  Windows (it should, but confirm). If CC uses `--allowed-directories` instead (flag name
  varies by version), the subprocess_mgr must use the correct flag name.
- ? `--allowedTools` tool name list: verify exact names CC uses (`Read`, `Write`, `Bash`,
  `Glob`, `Grep`, `LS` — capitalisation matters). Run `claude --help` to confirm.

**40.4:**
- ⚠ Plan-phase prompt engineering: the read-only phase prompt must instruct CC clearly
  that it should ONLY produce a plan, not execute it. CC in `-p` mode with read-only
  tools technically can't write files, but the prompt should reinforce this to avoid
  CC spending tokens trying and failing.
- ? Plan review approval timeout: 30s (the default) may be too short if the plan text
  is long and the user is reading it carefully. Consider a longer default for plan
  review requests (120s?), configurable via `plan_review_timeout_seconds` in CCConfig.

**40.5:**
- ⚠ The `cc_result` UniversalMessage type is new. Ensure the agent runtime handles it
  gracefully (doesn't crash on an unknown message type). Add it to the message type
  registry or handle with a default pass-through.
- ? Cost tracking: `cost_usd` from CC's JSON output should probably be logged to the
  audit log (not just returned in the result metadata). Add an `cc_session_cost` audit
  event so spending is traceable over time.
- ⚠ Slack/Email race condition noted in smoketest (PROGRESS.md): plan review and task
  approval requests sent via Slack channel have the same race condition as the existing
  SlackApprovalHandler. Do not rely on Slack for CC approvals until that race condition
  is resolved. CLI approval is safe.

---

## Structural Fences — Summary

Five fences in order of hardness. No single fence should be relied on alone.

| # | Fence | Type | Enforced by |
|---|-------|------|-------------|
| 1 | `--add-dir` restricts CC file access | Hard (CC-level) | CCSubprocessManager argv |
| 2 | `CLAUDE_CONFIG_DIR` isolates CC config | Hard (env) | WorkspaceManager.get_env() |
| 3 | git pre-push hook blocks push to main | Hard (git-level) | WorkspaceManager.create_project() |
| 4 | `DEAD_STOP_ACTIONS` blocks cc_bypass/cc_push_main | Hard (code) | existing ToolExecutor |
| 5 | CLAUDE.md in workspace projects | Soft (behavioral) | WorkspaceManager.write_project_claude_md() |

Defense in depth: fence ④ fires before any subprocess is spawned; fence ① fires at
the OS level even if fence ④ were somehow bypassed; fence ③ fires at push time even
if CC's working directory were somehow wrong.

---

## Approval Profile Behavior for CC Tools

| Profile   | Threshold | cc_read_analyze (5) | cc_write_task (3) | cc_run_with_shell (2) | cc_promote_request (1) |
|-----------|-----------|---------------------|-------------------|-----------------------|------------------------|
| paranoid  | 5         | requires approval   | requires approval | requires approval     | requires approval      |
| standard  | 3         | auto-approved       | requires approval | requires approval     | requires approval      |
| minimal   | 1         | auto-approved       | auto-approved     | auto-approved         | requires approval      |

`cc_promote_request` is always level 1 — it requires human approval under every profile.
This is the "wall" between the assistant's work and the human's main branch.

---

## Conventions for This Build

- The `claude_code/` module is a self-contained package. Nothing outside it should import
  from inside it except `startup.py` (which wires it) and `tools/registry.py` (which
  registers the tool definitions). No circular imports with the approval or security modules.
- CC subprocess args are built as a `list[str]` and validated before spawn — never
  interpolated into a shell string (no `shell=True`). This prevents shell injection.
- All CC-initiated git commits use the workspace-local git config (author set per-repo
  in `create_project`). The `GIT_AUTHOR_NAME` and `GIT_AUTHOR_EMAIL` env vars are also
  set in `get_env()` as a belt-and-suspenders measure.
- CC tool names in `--allowedTools` must be validated against a known-good list at startup
  (warn if an unrecognised tool name is in `exec_allowed_tools` config — likely a typo).
- Every CC session gets a UUID `session_id` logged to the audit trail (start, end, cost,
  duration). This is the correlation key if something goes wrong.

---

## Dependencies & Setup Prerequisites

- Claude Code CLI (`claude`) must be installed and accessible. Path configurable via
  `CCConfig.claude_binary`. Default assumes `claude` is on PATH.
- Windows user account `C:\Users\{assistant_name}\` must exist as a setup step
  (document in DEPLOY.md, not automated by the code).
- Git must be installed and on PATH (already required by OpenRattler).
- No new Python packages required — `asyncio.create_subprocess_exec` is stdlib.

---

## Status

Current milestone: **40.5 — complete**  
Piece 40.1: ✅ complete — merged  
Piece 40.2: ✅ complete — merged  
Piece 40.3: ✅ complete — merged  
Piece 40.4: ✅ complete — merged  
Piece 40.5: ✅ complete — PR open  
Last updated: 2026-04-21

---

---

# Build Plan: Google Workspace Integration (41.x)

Generated by /planner on 2026-04-23
Follows: Build Pieces 40.1–40.5 (Claude Code Integration — complete)

---

## What We're Building

Read-and-write access to Google Calendar, Google Drive, Gmail, and Google Tasks — four
Google Workspace services the assistant will interact with daily. The design is deliberately
limited: the assistant can read and add, but never delete. Gmail gets one additional
operation (archive) but not permanent delete. Tasks gets one additional operation
(mark complete) but not delete.

**Scope note:** The OAuth scope used is `calendar.events` (not the broader `calendar`
scope), which covers event read/write but not the calendar list endpoint. All calendar
tools use a known `calendar_id` (defaulting to `"primary"`) — listing available calendars
is not supported under this scope and is not included in the tool set.

Every inbound result travels through a security sanitization layer before it reaches
the agent context. Email and Drive file content is treated as attacker-controlled data
and scanned for prompt injection patterns before being wrapped in a UniversalMessage
with `trust_level="public"`. The assistant's own calendar and folder listings carry
`trust_level="local"`.

---

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│  INBOUND: Reading from Google                                        │
│                                                                      │
│  Google API (Calendar / Drive / Gmail)                               │
│       │                                                              │
│       │  raw API response                                            │
│       ▼                                                              │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  GoogleContentSanitizer  (41.2 — built before any read tool) │   │
│  │                                                               │   │
│  │  • Strip HTML → plain text                                    │   │
│  │  • Truncate at configured char limit                          │   │
│  │  • scan_for_suspicious_content() from security/patterns.py   │   │
│  │    ┌─ CLEAN ──► [EMAIL CONTENT]...[/EMAIL CONTENT]           │   │
│  │    │             (or [FILE CONTENT], [EVENT CONTENT])         │   │
│  │    └─ FLAGGED ─► [SECURITY WARNING: {categories}] prepended  │   │
│  │                   still passed (not silently dropped)         │   │
│  │                   + audit: security_content_flagged           │   │
│  │  • Sender / metadata always passed separately from body       │   │
│  └──────────────────────────────────────────────────────────────┘   │
│       │                                                              │
│       ▼                                                              │
│  Tool handler builds UniversalMessage:                               │
│    type        = "response"                                          │
│    operation   = tool name (e.g. "gmail_read_thread")                │
│    trust_level = "public"  ← email, Drive file content (external)   │
│                  "local"   ← calendar events, folder listings        │
│    params      = structured sanitized data                           │
│    trace_id    = propagated from ToolCall                            │
│       │                                                              │
│       ▼                                                              │
│  ToolResult(result=UniversalMessage) → agent runtime                 │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  OUTBOUND: Writing to Google                                         │
│                                                                      │
│  Agent calls tool (e.g. calendar_create_event)                       │
│       │                                                              │
│       ▼                                                              │
│  ToolExecutor (existing 39.x gate)                                   │
│  • Dead stop check (no delete tools exist — nothing to stop)         │
│  • Threshold check → approval if level ≤ threshold                   │
│  • Approval prompt: action + target + level + agent rationale        │
│       │  (approved)                                                  │
│       ▼                                                              │
│  Google API call                                                      │
│  • Calendar events: description includes "[Created by {assistant name}]" ("OpenRattler" default)  │
│  • Drive uploads: OpenRattler attribution in file metadata           │
│  • Gmail mutations: full audit log entry for every label/archive op  │
│  • Tasks mutations: audit log entry on create/complete             │
│       │                                                              │
│       ▼                                                              │
│  ToolResult(result=UniversalMessage) → agent runtime                 │
└─────────────────────────────────────────────────────────────────────┘
```

### Component Map

```
openrattler/
├── integrations/                  ← NEW top-level package
│   └── google/
│       ├── __init__.py
│       ├── auth.py                ← GoogleAuthManager: OAuth flow, token refresh
│       ├── client.py              ← GoogleClientFactory: Calendar/Drive/Gmail service builders
│       └── sanitizer.py           ← GoogleContentSanitizer: prompt injection defense
├── tools/
│   └── builtin/
│       ├── calendar_tools.py      ← ToolDefinitions + CalendarToolHandler
│       ├── drive_tools.py         ← ToolDefinitions + DriveToolHandler
│       ├── gmail_tools.py         ← ToolDefinitions + GmailToolHandler
│       └── tasks_tools.py         ← ToolDefinitions + TasksToolHandler
├── config/
│   └── loader.py                  ← add GoogleConfig
└── cli/
    └── main.py                    ← add `openrattler google-auth` command
```

The `integrations/google/` package owns the API client layer — auth, service objects,
and content security. The `tools/builtin/` files own the tool definitions and handlers.
This mirrors the separation in `tools/claude_code/` (integration layer) vs. the
`tools/builtin/` pattern (tool surface). Nothing outside `startup.py` and the builtin
tool files should import from `integrations/google/`.

---

## Tool Definitions and Action Levels

### Google Calendar

| Tool | Level | Description |
|------|-------|-------------|
| `calendar_list_events` | 5 | List events in a date range |
| `calendar_read_event` | 5 | Full details of a specific event |
| `calendar_create_event` | 3 | Create a new event (modifies external state) |

No update, no delete. `calendar_list_calendars` is excluded — the `calendar.events`
scope does not grant access to the calendar list endpoint. All tools accept a
`calendar_id` parameter (default `"primary"`).

### Google Tasks

| Tool | Level | Description |
|------|-------|-------------|
| `tasks_list_tasklists` | 5 | List all task lists |
| `tasks_list_tasks` | 5 | List tasks in a task list |
| `tasks_read_task` | 5 | Full details of a specific task |
| `tasks_create_task` | 3 | Create a new task |
| `tasks_complete_task` | 4 | Mark a task complete or incomplete (reversible) |

No delete. `tasks_complete_task` is level 4 (minor, reversible) — toggling completion
state is a routine workflow action that doesn't warrant level-3 weight.

### Google Drive

| Tool | Level | Description |
|------|-------|-------------|
| `drive_list_files` | 5 | Query files by name/type/folder |
| `drive_list_folders` | 5 | List folders |
| `drive_read_file` | 5 | Read file content (sanitized; text/Doc types only) |
| `drive_create_folder` | 3 | Create a new folder |
| `drive_upload_file` | 3 | Upload a new file |

No rename, no update, no delete.

### Gmail

| Tool | Level | Description |
|------|-------|-------------|
| `gmail_list_labels` | 5 | List all available labels |
| `gmail_list_threads` | 5 | Query inbox threads |
| `gmail_read_thread` | 5 | Sanitized thread content + separate metadata |
| `gmail_star_message` | 4 | Apply or remove star marker |
| `gmail_mark_important` | 4 | Apply or remove important marker |
| `gmail_apply_label` | 4 | Apply a label to a thread |
| `gmail_remove_label` | 4 | Remove a label from a thread |
| `gmail_create_label` | 4 | Create a new label |
| `gmail_archive` | 2 | Remove thread from inbox (significant — harder to find) |

No mark-read, no send, no delete. `gmail_archive` is level 2 because while technically
reversible, a silently archived email is easy to lose track of — it requires the same
weight of consideration as other "significant, hard to notice" actions in the system.

### Action Level Rationale

All read operations are level 5 (auto-approved under standard and minimal profiles —
the assistant should be able to look at your calendar without interrupting you).
Calendar creates, Drive uploads, and task creates are level 3 (moderate — they create
external state you'd need to clean up manually). Gmail label operations, task completion
toggles, and star/important markers are level 4 (minor, easily reversed). Archive is
level 2 (significant — same tier as pushing a git branch).

---

## Data Flow: UniversalMessage as Interchange

Every tool handler — read or write — returns a `ToolResult` whose `result` field
contains a `UniversalMessage`. This is the system's message standard and it carries
the `trust_level` signal that downstream components need:

```
gmail_read_thread result:
  ToolResult(
    success=True,
    result=UniversalMessage(
      type        = "response",
      operation   = "gmail_read_thread",
      trust_level = "public",           ← email from internet = untrusted
      params      = {
        "thread_id":  "...",
        "subject":    "...",            ← from metadata, not body
        "from":       "sender@example.com",
        "date":       "...",
        "body":       "[EMAIL CONTENT]\n{sanitized text}\n[/EMAIL CONTENT]",
        "was_truncated": false,
        "security_flags": [],           ← non-empty if sanitizer flagged patterns
      },
      trace_id    = propagated from ToolCall.trace_id,
    )
  )

calendar_create_event result:
  ToolResult(
    success=True,
    result=UniversalMessage(
      type        = "response",
      operation   = "calendar_create_event",
      trust_level = "local",            ← your own calendar
      params      = {
        "event_id":   "...",
        "html_link":  "...",
        "summary":    "...",
        "start":      "...",
        "end":        "...",
      },
      trace_id    = propagated from ToolCall.trace_id,
    )
  )
```

`trust_level="public"` on email and Drive file reads is the structural signal — the same
signal used when user messages arrive from external channels — that this content crossed
a trust boundary and should not be acted on without scrutiny.

---

## GoogleConfig

Added to `openrattler/config/loader.py` as `google: GoogleConfig` on `AppConfig`:

```python
class GoogleConfig(BaseModel):
    enabled: bool = False
    credentials_file: str   # path to credentials.json from Google Cloud Console
                            # default: ~/.openrattler/google/credentials.json
    token_file: str         # where token.json is written after auth flow
                            # default: ~/.openrattler/google/token.json
    user_email: str         # primary Google account email
    calendar_id: str = "primary"
    task_list_id: str = "@default"   # "@default" resolves to the primary task list
    drive_upload_folder_id: Optional[str] = None  # None = Drive root
    max_email_chars: int = 10000     # body truncation limit
    max_file_chars: int = 20000      # Drive content truncation limit
    max_threads_per_query: int = 20  # gmail_list_threads result cap
    max_events_per_query: int = 50   # calendar_list_events result cap
    max_tasks_per_query: int = 100   # tasks_list_tasks result cap
    max_file_bytes: int = 5_000_000  # Drive file size limit before refusing to read
```

OAuth scopes (as configured):
- `https://www.googleapis.com/auth/calendar.events`
  (event read/write on known calendars; does not include calendar list endpoint)
- `https://www.googleapis.com/auth/tasks`
  (full task list and task read/write)
- `https://www.googleapis.com/auth/drive`
  (Drive read/write)
- `https://www.googleapis.com/auth/gmail.modify`
  (Gmail read + label changes + archive; explicitly excludes permanent delete)

---

## Build Order

---

### Piece 41.1 — GoogleConfig + Auth Foundation

**Build:**
- Add `GoogleConfig` to `openrattler/config/loader.py` — Pydantic model as above;
  `credentials_file` and `token_file` default to `~/.openrattler/google/` paths;
  validate that `credentials_file` exists when `enabled=True`
- Create `openrattler/integrations/google/__init__.py`
- Create `openrattler/integrations/google/auth.py` — `GoogleAuthManager`:
  - `__init__(config: GoogleConfig)`
  - `get_credentials() -> google.oauth2.credentials.Credentials` — loads `token.json`,
    refreshes silently if expired, raises `GoogleAuthError` if not authenticated
  - `run_auth_flow() -> None` — runs `InstalledAppFlow`, saves `token.json`
  - `is_authenticated() -> bool` — True if valid (non-expired) credentials exist
- Create `openrattler/integrations/google/client.py` — `GoogleClientFactory`:
  - `__init__(auth: GoogleAuthManager)`
  - `calendar_service()` — returns `build("calendar", "v3", credentials=...)`; cached
  - `drive_service()` — returns `build("drive", "v3", credentials=...)`; cached
  - `gmail_service()` — returns `build("gmail", "v1", credentials=...)`; cached
  - `tasks_service()` — returns `build("tasks", "v1", credentials=...)`; cached
  - All four lazily initialized and cached on first call
- Add `openrattler google-auth` CLI command in `cli/main.py` — calls
  `GoogleAuthManager(config).run_auth_flow()`, prints success message with
  `~/.openrattler/google/token.json` location
- Tests in `tests/test_integrations/test_google_auth.py`:
  - `is_authenticated()` returns False when `token.json` absent
  - `get_credentials()` raises `GoogleAuthError` when not authenticated
  - Expired credentials trigger refresh (mock `google.oauth2.credentials.Credentials.refresh`)
  - `GoogleClientFactory` returns cached service on second call (assert build() called once)
    — test for all four services (calendar, drive, gmail, tasks)
  - `GoogleConfig` validation: error when `enabled=True` and `credentials_file` missing

**Why first:** Everything in 41.2–41.6 needs a working client. Auth is pure I/O with no
dependencies on other new modules, so failures here are easy to isolate. The `google-auth`
CLI command also lets you complete the one-time OAuth flow before any tool code exists.

**New dependency:** `google-api-python-client`, `google-auth-oauthlib`, `google-auth-httplib2`
(add to `pyproject.toml` dependencies)

**Done when:** `pytest`, `mypy`, `black` pass. `openrattler google-auth` runs the OAuth
flow and writes `token.json`. `GoogleClientFactory` returns service objects without error.

---

### Piece 41.2 — GoogleContentSanitizer

**Build:**
- Create `openrattler/integrations/google/sanitizer.py`:
  - `SanitizedContent` dataclass: `text: str`, `was_truncated: bool`,
    `security_flags: list[tuple[str, str]]`, `original_length: int`
  - `GoogleContentSanitizer`:
    - `__init__(config: GoogleConfig, audit: AuditLog)`
    - `sanitize_email_body(raw: str, mime_type: str) -> SanitizedContent`:
      - If `mime_type` is `text/html`: strip tags to plain text (use `html.parser` from
        stdlib — no new dependency)
      - Truncate at `config.max_email_chars`
      - `scan_for_suspicious_content(text)` from `openrattler.security.patterns`
      - If flags found: `audit.log(AuditEvent(event="security_content_flagged", ...))`
      - Return `SanitizedContent`
    - `sanitize_file_content(raw: str, mime_type: str) -> SanitizedContent`:
      - Same pipeline, `config.max_file_chars` limit
    - `wrap_for_agent(content: SanitizedContent, label: str) -> str`:
      - Clean: `[{LABEL}]\n{content.text}\n[/{LABEL}]`
      - Flagged: prefix with `[SECURITY WARNING: suspicious content detected —
        categories: {", ".join(cat for cat, _ in flags)}]\n`
      - Truncation note appended if `was_truncated`: `\n[Content truncated at
        {config.max_*_chars} characters]`
- Tests in `tests/test_integrations/test_google_sanitizer.py`:
  - HTML stripped to plain text (anchor tags, bold, scripts removed)
  - Truncation at configured limit; `was_truncated=True` set
  - Known injection pattern triggers `security_flags` and audit event
  - Clean content has empty `security_flags`
  - `wrap_for_agent` with flags prepends `[SECURITY WARNING]` block
  - `wrap_for_agent` without flags has no warning block
  - Sender info in `params` dict is never passed through `wrap_for_agent`
    (test that it arrives in `params.from`, not in `params.body`)

**Why second:** No reading tool should be built before its security gate exists. Making
this its own piece means the sanitizer gets a dedicated PR and test suite — not patched
in as an afterthought. This is the most security-critical piece of the 41.x series.

**Done when:** `pytest`, `mypy`, `black` pass. Prompt injection strings from
`SUSPICIOUS_PATTERNS` are detected and flagged. Audit event fires on flag.

---

### Piece 41.3 — Google Calendar Tools

**Build:**
- Create `openrattler/tools/builtin/calendar_tools.py`:
  - Three `ToolDefinition` instances with parameter schemas:
    - `calendar_list_events` — `time_min: str (ISO8601)`, `time_max: str (ISO8601)`,
      `calendar_id: str = "primary"`, `max_results: int = 50`
    - `calendar_read_event` — `event_id: str`, `calendar_id: str = "primary"`
    - `calendar_create_event` — `summary: str`, `start: str (ISO8601)`, `end: str (ISO8601)`,
      `description: Optional[str]`, `location: Optional[str]`,
      `calendar_id: str = "primary"`
  - Note: `calendar_list_calendars` is intentionally absent — the `calendar.events` scope
    does not grant access to `calendarList.list`. All tools accept `calendar_id` (default
    `"primary"`) so the agent can target other calendars by ID if the user provides one.
  - `CalendarToolHandler`:
    - `__init__(client: GoogleClientFactory, config: GoogleConfig, audit: AuditLog)`
    - One method per tool; each returns `ToolResult`
    - All read operations: `trust_level="local"`, structured event data in `params`
    - `handle_create_event`: appends `"\n\n[Created by {assistant name} ("OpenRattler" default)]"` to
      description before API call; audit log entry on success
    - All handlers: build `UniversalMessage` with `trace_id` propagated from `ToolCall`
    - All handlers: return `ToolResult(call_id=..., success=True, result=UniversalMessage)`
      on success; `ToolResult(call_id=..., success=False, error=str(exc))` on failure
  - `register_calendar_tools(registry: ToolRegistry, handler: CalendarToolHandler) -> None`
    — registers all three tools with their handlers; called from `startup.py` when
    `config.google.enabled`
- Tests in `tests/test_tools/test_calendar_tools.py`:
  - All three handlers return `ToolResult` with `UniversalMessage` in `result`
  - `trust_level="local"` on all results
  - `calendar_create_event` result UM `params` contains `event_id`
  - `calendar_create_event` description includes attribution string
  - Google API exception → `ToolResult(success=False, error=...)`
  - Tools not registered when `google.enabled=False`

**Done when:** `pytest`, `mypy`, `black` pass. All three tools registered and callable
via the executor. `trace_id` flows through to the result UM.

---

### Piece 41.4 — Google Drive Tools

**Build:**
- Create `openrattler/tools/builtin/drive_tools.py`:
  - Five `ToolDefinition` instances:
    - `drive_list_files` — `query: Optional[str]`, `folder_id: Optional[str]`,
      `max_results: int = 20`, `mime_type: Optional[str]`
    - `drive_list_folders` — `parent_id: Optional[str]`, `max_results: int = 20`
    - `drive_read_file` — `file_id: str`
    - `drive_create_folder` — `name: str`, `parent_id: Optional[str]`
    - `drive_upload_file` — `name: str`, `content: str`, `mime_type: str`,
      `folder_id: Optional[str]`
  - `DriveToolHandler`:
    - `__init__(client: GoogleClientFactory, sanitizer: GoogleContentSanitizer,
      config: GoogleConfig, audit: AuditLog)`
    - `handle_read_file`: exports Google Docs as `text/plain` via Drive export API;
      for other text/* types, downloads content directly; passes through sanitizer;
      `trust_level="public"` (file may contain content from any source)
    - `handle_list_*`: `trust_level="local"` (your own folder structure)
    - `handle_upload_file`: sets `appProperties={"created_by": "{assistant name}" ("OpenRattler" default)}` on
      upload; `trust_level="local"` on result UM
    - `handle_create_folder`: `trust_level="local"` on result UM; audit log entry
    - Non-text file types in `handle_read_file` (PDF, images, etc.): return
      `ToolResult(success=False, error="drive_read_file supports text and Google Doc
      files only — binary file types are not supported")`
  - `register_drive_tools(registry, handler)` — same pattern as calendar
- Tests in `tests/test_tools/test_drive_tools.py`:
  - All five handlers return `ToolResult` with `UniversalMessage`
  - `drive_read_file` result has `trust_level="public"`, sanitizer was called
  - `drive_list_files` result has `trust_level="local"`
  - Google Doc export called for `application/vnd.google-apps.document` mime type
  - Unsupported binary mime type returns `success=False` with clear error
  - `drive_upload_file` sets `appProperties` attribution

**Done when:** `pytest`, `mypy`, `black` pass. File read passes through sanitizer.
Drive listing results are structured with correct trust levels.

---

### Piece 41.5 — Gmail Tools

**Build:**
- Create `openrattler/tools/builtin/gmail_tools.py`:
  - Nine `ToolDefinition` instances (see tool table above) with parameter schemas:
    - `gmail_list_threads` — `query: str`, `max_results: int = 20`
    - `gmail_read_thread` — `thread_id: str`
    - `gmail_star_message` — `message_id: str`, `starred: bool`
    - `gmail_mark_important` — `message_id: str`, `important: bool`
    - `gmail_apply_label` — `thread_id: str`, `label_id: str`
    - `gmail_remove_label` — `thread_id: str`, `label_id: str`
    - `gmail_create_label` — `name: str`, `label_list_visibility: str = "labelShow"`,
      `message_list_visibility: str = "show"`
    - `gmail_archive` — `thread_id: str`
    - `gmail_list_labels` — no params
  - `GmailToolHandler`:
    - `__init__(client: GoogleClientFactory, sanitizer: GoogleContentSanitizer,
      config: GoogleConfig, audit: AuditLog)`
    - `handle_read_thread`: fetches thread with `format="full"`; walks MIME parts to
      extract text/plain body (prefers text/plain over text/html; decodes base64url);
      passes body through `sanitizer.sanitize_email_body()`; builds UM with:
      - `params.subject`, `params.from`, `params.date` from headers (NOT body)
      - `params.body` = `sanitizer.wrap_for_agent(sanitized, "EMAIL CONTENT")`
      - `params.thread_id`, `params.message_count`
      - `trust_level="public"`
    - `handle_archive`: removes `"INBOX"` label via `modify`; audit log entry with
      `thread_id`, `subject` (from metadata fetch); `trust_level="local"` on result UM
    - `handle_create_label`: audit log entry; `trust_level="local"` on result UM
    - All label/star/important handlers: audit log entry on success; `trust_level="local"`
  - `register_gmail_tools(registry, handler)` — same pattern
- Tests in `tests/test_tools/test_gmail_tools.py`:
  - `gmail_read_thread` result UM has `trust_level="public"`, sanitizer was called
  - Subject/from/date in `params` directly, NOT embedded in body string
  - MIME multipart traversal extracts text/plain correctly
  - base64url body decoded before sanitization
  - Injection pattern in body → `security_flags` in result UM `params`, audit event fired
  - `gmail_archive` result UM has `trust_level="local"`, audit event fired
  - `gmail_list_labels` returns label list with `trust_level="local"`
  - All nine tools registered when `google.enabled=True`; none registered when False

**Why second-to-last:** Gmail is the highest-complexity service: MIME parsing, base64
decoding, thread vs. message distinction, and the highest prompt injection surface.
Building it after the sanitizer (41.2) and after the simpler Calendar and Drive patterns
are established means the MIME plumbing is the only genuinely new challenge.

**Done when:** `pytest`, `mypy`, `black` pass. MIME body extracted and sanitized.
`gmail_archive` goes through the approval gate at level 2 under standard profile.
Injection string in email body triggers `[SECURITY WARNING]` wrapper in result UM.

---

### Piece 41.6 — Google Tasks Tools

**Build:**
- Create `openrattler/tools/builtin/tasks_tools.py`:
  - Five `ToolDefinition` instances with parameter schemas:
    - `tasks_list_tasklists` — no required params
    - `tasks_list_tasks` — `tasklist_id: str = "@default"`, `show_completed: bool = False`,
      `show_hidden: bool = False`, `max_results: int = 100`
    - `tasks_read_task` — `task_id: str`, `tasklist_id: str = "@default"`
    - `tasks_create_task` — `title: str`, `notes: Optional[str]`, `due: Optional[str (ISO8601)]`,
      `tasklist_id: str = "@default"`, `parent: Optional[str]` (parent task_id for subtasks)
    - `tasks_complete_task` — `task_id: str`, `tasklist_id: str = "@default"`,
      `completed: bool = True` (False to un-complete)
  - `TasksToolHandler`:
    - `__init__(client: GoogleClientFactory, sanitizer: GoogleContentSanitizer,
      config: GoogleConfig, audit: AuditLog)`
    - `handle_list_tasklists`: returns task list metadata; `trust_level="local"`
    - `handle_list_tasks`: returns task summaries (title, due, status, id);
      task `notes` field passed through `sanitizer.sanitize_file_content()` if present
      (notes are user-authored but could contain externally pasted content);
      `trust_level="local"`
    - `handle_read_task`: returns full task; `notes` sanitized; `trust_level="local"`
    - `handle_create_task`: appends `" [{assistant name}]" ("OpenRattler" default)"` suffix to title so assistant-created
      tasks are identifiable; audit log entry on success; `trust_level="local"` on result UM
    - `handle_complete_task`: calls `tasks.patch()` with `{"status": "completed"}` or
      `{"status": "needsAction", "completed": None}`; audit log entry; `trust_level="local"`
    - All handlers: build `UniversalMessage` with `trace_id` propagated from `ToolCall`
  - `register_tasks_tools(registry, handler)` — same pattern as other services
- Tests in `tests/test_tools/test_tasks_tools.py`:
  - All five handlers return `ToolResult` with `UniversalMessage`
  - `trust_level="local"` on all results
  - `handle_create_task` title includes `" [{assistant name}]" ("OpenRattler" default)"` suffix
  - `handle_complete_task` with `completed=True` sends `status="completed"` to API
  - `handle_complete_task` with `completed=False` sends `status="needsAction"` to API
  - Task `notes` field passed through sanitizer
  - Audit log entry fired on create and complete
  - Tools not registered when `google.enabled=False`

**Why after Gmail:** Tasks is simpler than Gmail (no MIME, no injection surface as severe
as external email). Building it last means the pattern is well-established and the piece
is straightforward — a clean end to the 41.x series.

**Done when:** `pytest`, `mypy`, `black` pass. All five tools registered. `tasks_complete_task`
audit log entry verified. `tasks_create_task` title suffix present in API call.

---

## Edge Cases & Open Questions

**41.1:**
- ⚠ `credentials_file` must exist before the first `openrattler google-auth` run.
  Document this in DEPLOY.md: download from Google Cloud Console, place at
  `~/.openrattler/google/credentials.json` before running `google-auth`.
- ⚠ `token.json` contains a refresh token — treat it like a password. Add to `.gitignore`.
  Document this explicitly in setup instructions.
- ? Scopes are fixed at auth-flow time. If a scope is added later (e.g. a future
  Google service), the user will need to re-run `openrattler google-auth` to
  re-consent. Document this as a known operational fact.

**41.2:**
- ⚠ `html.parser` from stdlib strips tags but doesn't decode HTML entities (e.g. `&amp;`
  → `&`). Use `html.unescape()` after stripping to avoid garbled plain text.
- ? `scan_for_suspicious_content()` was designed for command text — verify it fires on
  typical prompt injection phrases ("ignore previous instructions", "you are now", etc.)
  before 41.2 is considered done. If coverage is insufficient, discuss extending
  `SUSPICIOUS_PATTERNS` in a separate PR.
- ⚠ Security flags don't block delivery (content still reaches the agent with a warning).
  This is intentional: silently dropping flagged emails could itself be exploited to
  suppress legitimate messages. Confirm this behavior is acceptable before 41.5 builds.

**41.3:**
- ? `calendar_create_event` recurrence support (RRULE) is not in scope for 41.3.
  If needed later, it's a parameter addition, not an architectural change.
- ⚠ Google Calendar API returns datetimes in various timezone formats. Normalize to ISO
  8601 UTC in the result UM `params` before returning to the agent.

**41.4:**
- ⚠ Google Docs export as text/plain loses formatting (headers, bullets, tables become
  flat text). This is acceptable for the assistant's read use case. Document the
  limitation in the tool description.
- ⚠ Large files: `max_file_bytes` (default 5MB) is checked before fetching content —
  return `ToolResult(success=False, error="File too large...")` if the file size metadata
  exceeds the limit. `max_file_bytes` is already in `GoogleConfig`.

**41.6:**
- ⚠ `"@default"` resolves to the user's primary task list. If the user has multiple task
  lists and wants to specify one by name, `tasks_list_tasklists` returns the IDs needed.
  The `config.task_list_id` default covers the common single-list case.
- ? Subtask support: `tasks_create_task` accepts a `parent` task_id for creating subtasks.
  The Google Tasks API returns subtasks only when explicitly fetching a parent's children —
  `tasks_list_tasks` does not return subtasks by default. Document this behavior in the
  tool description so the agent knows to check subtasks separately.
- ⚠ Tasks `notes` field may be None for many tasks — the sanitizer call must handle
  `None` input gracefully (return empty `SanitizedContent` rather than raising).

**41.5:**
- ⚠ Gmail threads can have many messages. `gmail_read_thread` should return the most
  recent N messages (configurable, default 5) when a thread is long, with a note in
  `params.truncation_note` if older messages were omitted.
- ⚠ MIME structure varies widely (multipart/alternative, multipart/mixed, nested parts).
  The MIME walker must handle arbitrarily nested structures, not just one level deep.
- ? `gmail_list_threads` returns thread IDs and snippets. Snippets are short plain-text
  previews from Google — they are pre-extracted and do not require full sanitization,
  but should still be passed through `scan_for_suspicious_content()` since they are
  externally sourced.

---

## Dependencies & Setup Prerequisites

**New Python packages** (add to `pyproject.toml`):
- `google-api-python-client`
- `google-auth-oauthlib`
- `google-auth-httplib2`

**One-time Google Cloud setup** (✅ already completed by user):
1. ✅ Google Cloud project created
2. ✅ APIs enabled: Google Calendar API, Google Drive API, Gmail API, Google Tasks API
3. ✅ OAuth consent screen configured, `paularthurmiller@gmail.com` as test user
4. ✅ Scopes added: `calendar.events`, `tasks`, `drive`, `gmail.modify`
5. ✅ OAuth 2.0 credential (Desktop app) created, `credentials.json` downloaded
6. Place `credentials.json` at `~/.openrattler/google/credentials.json`
7. Add `~/.openrattler/google/token.json` to `.gitignore`

After 41.1 is built: run `openrattler google-auth` once to complete the OAuth flow.

---

## Conventions for This Build

- Every tool handler returns `ToolResult(result=UniversalMessage)` — never a raw dict.
  This is the message standard and the `trust_level` field is load-bearing.
- `trust_level="public"` for all content that originated from outside the user's control
  (email bodies, Drive file content). `trust_level="local"` for structural data that
  is the user's own (calendar listings, folder listings, label lists, operation results).
- Sender/author metadata always lives in `params` at the same level as `params.body`,
  never embedded inside the body string. This prevents injection via crafted From headers.
- All write operations (create, label, archive) produce an audit log entry. Read
  operations at level 5 do not (they're auto-approved and high-volume).
- The `integrations/google/` package is self-contained. Nothing outside of `startup.py`,
  `calendar_tools.py`, `drive_tools.py`, `gmail_tools.py`, and `tasks_tools.py` should
  import from it.
- Tool registration is always gated on `config.google.enabled` — no Google imports at
  startup when disabled. Same lazy-import pattern as `tools/claude_code/`.

---

## Approval Profile Behavior for Google Tools

| Profile  | Threshold | Reads (5) | Create/Upload (3) | Label/Star/Complete (4) | Archive (2) |
|----------|-----------|-----------|-------------------|-------------------------|-------------|
| paranoid | 5         | approval  | approval          | approval                | approval    |
| standard | 3         | auto      | approval          | auto                    | approval    |
| minimal  | 1         | auto      | auto              | auto                    | approval    |

`gmail_archive` (level 2) requires approval under every profile. This is intentional —
archiving email is a real action with real consequences and should always have a human
sign-off. `tasks_complete_task` is level 4 (auto under standard/minimal) — toggling
completion state is routine enough that it should not interrupt the user.

---

## Status

Current milestone: **41.6 — complete**
Piece 41.1: ✅ complete — PR open
Piece 41.2: ✅ complete — PR open
Piece 41.3: ✅ complete — PR open
Piece 41.4: ✅ complete — PR open
Piece 41.5: ✅ complete — PR open
Piece 41.6: ✅ complete — PR open
Last updated: 2026-04-24

---

# Build 42 — Bayesian Calibration Loop Prerequisites

Planned: 2026-05-19

These three pieces are the load-bearing prerequisites for Build Piece 20.1 (Bayesian heartbeat
evaluation loop, per BUILD_GUIDE.md backlog). The CalibrationAgent cannot be built until all
three are merged and working.

## Architecture

```
ToolExecutor (enhanced: agent_config injection)
       │
       ├── session_read_self (action_level=5) ── reads caller's own session
       │       └── TranscriptStore.load_since()
       │               └── (summarize=True) → SummarizerAgent
       │
       └── session_lookup (action_level=3) ──── cross-session reads
               ├── handler: CalibrationAgent scope check (session_key + time window)
               ├── TranscriptStore.load_since()
               ├── AuditLog (every read logged)
               └── (summarize=True) → SummarizerAgent

SummarizerAgent (standalone, no tool/session deps)
  └── skill selection: SKILL_{name}.md → fallback SKILL.md
  └── LLM call (tools=None), output scan, response UM
```

**Key security decisions:**
- Two tools (not one) because `action_level` is static — self-reads are level 5 (auto),
  cross-session reads are level 3 (approval-gated for main agent)
- CalibrationAgent bypass: heartbeat sessions auto-bypass the approval gate via
  `heartbeat_bypass_ops`; handler still enforces scope+time-window as defense-in-depth
- `agent_config` injected at executor level (not LLM-visible, excluded from tool schema
  via `_RUNTIME_PARAMS`) — this is the mechanism by which handlers know their caller

---

## Build Order

---

### Piece 42.1 — SummarizerAgent

**Build:**
- Create `openrattler/agents/summarizer/` package (mirrors ResearchAgent structure):
  - `__init__.py`
  - `config.py` — `SummarizerAgentConfig` (Pydantic):
    - `model: str = "claude-haiku-4-5-20251001"` — cheap model, configurable
    - `skill_prompt_path: Path` — must exist; loaded at init (raises `FileNotFoundError` otherwise)
    - `named_skills_dir: Optional[Path] = None` — directory of `SKILL_*.md` named skills
    - `max_input_chars: int = 100_000` — truncation limit before LLM call
    - `max_output_chars: int = 4_000` — max summary length returned
  - `models.py`:
    - `SummarizerRequest`: `text: str`, `skill: Optional[str] = None`,
      `context: Optional[str] = None`
    - `SummarizerResult`: `summary: str`, `original_length: int`, `skill_used: str`,
      `was_truncated: bool`
    - `SummarizerError`: `error_code: str`, `message: str`, `trace_id: str`
  - `agent.py` — `SummarizerAgent`:
    - `__init__(config, audit, provider=None)` — reads skill prompts at init; raises
      `FileNotFoundError` if `skill_prompt_path` missing (same pattern as ResearchAgent)
    - `run(um, trace_id, from_agent, to_agent, session_key) -> UniversalMessage`:
      1. Parse `SummarizerRequest` from `um.params`; return error UM if invalid
      2. Select skill: if `skill` set and `SKILL_{skill}.md` exists in `named_skills_dir` →
         use it; else fallback to default `skill_prompt_path`
      3. Truncate `text` at `max_input_chars`; set `was_truncated=True`
      4. Build `[{system: skill_prompt}, {user: text + optional context}]`
      5. LLM call `tools=None`; stub fallback (first ~1000 chars) if provider None
      6. `scan_for_suspicious_content(output)` — if flagged: audit event + error UM
         with `error_code="OUTPUT_FLAGGED"`
      7. Return response UM with `SummarizerResult` in params
    - Never raises; all errors → error-type UM
  - `prompts/SKILL.md` — general-purpose system prompt: concise synthesis, preserve key
    facts, flag gaps, analyst voice, no verbatim repetition
  - `prompts/SKILL_session.md` — session transcript skill: focus on topics discussed,
    decisions made, user preferences expressed, action items
- Tests in `tests/test_agents/test_summarizer/test_summarizer_agent.py`:
  - Default skill loaded when `skill=None`
  - Named skill loaded when `SKILL_session.md` exists and `skill="session"`
  - Falls back to default when named skill file missing
  - Input truncated at `max_input_chars`; `was_truncated=True` set
  - Suspicious output → error UM with `error_code="OUTPUT_FLAGGED"`; audit event fired
  - No provider → stub summary returned (no crash)
  - `FileNotFoundError` at init if `skill_prompt_path` missing
  - Invalid UM params → error UM, not crash
  - Always returns UM; never raises

**Why first:** Zero dependencies on 42.2 or 42.3. Build and test it in isolation before
wiring into session_lookup.

**New files:**
- `openrattler/agents/summarizer/__init__.py`
- `openrattler/agents/summarizer/config.py`
- `openrattler/agents/summarizer/models.py`
- `openrattler/agents/summarizer/agent.py`
- `openrattler/agents/summarizer/prompts/SKILL.md`
- `openrattler/agents/summarizer/prompts/SKILL_session.md`
- `tests/test_agents/test_summarizer/__init__.py`
- `tests/test_agents/test_summarizer/test_summarizer_agent.py`

**Implementation note:** Strip `"anthropic/"` prefix before passing model to provider —
same as `model.removeprefix("anthropic/")` in ResearchAgent.

**Done when:** `pytest`, `mypy`, `black` pass. Stub + real provider paths covered. Named
skill selection and fallback tested. Suspicious output detection tested.

---

### Piece 42.2 — TranscriptStore time-range + executor agent_config injection

**Build:**
- `openrattler/storage/transcripts.py` — add:
  ```python
  async def load_since(
      self,
      session_key: str,
      since_iso: str,
      max_turns: int = 20,
  ) -> list[UniversalMessage]:
  ```
  - Validate `session_key` via existing `_validate_session_key`
  - Parse `since_iso` as UTC-aware `datetime`; raise `ValueError` if not parseable
    (normalize naive strings to UTC before comparison)
  - Read all lines; deserialize each UM; filter `message.timestamp >= since_dt`
  - Apply `max_turns` cap: take last `max_turns` from filtered list (oldest-first output)
  - Return empty list if file doesn't exist (same contract as `load_recent`)

- `openrattler/tools/executor.py` — extend step 7 (handler invocation):
  - After resolving `handler`, check `"agent_config" in inspect.signature(handler).parameters`
  - If present: add `agent_config=agent_config` to the kwargs dict before calling
  - `inspect.signature` is CPython-cached per function — negligible hot-path overhead
  - No change to any existing handler (none declare `agent_config`)

- Tests (additions to existing test files):
  - `tests/test_storage/test_transcripts.py`:
    - `load_since` cuts out messages older than the given timestamp
    - `load_since` applies `max_turns` cap after time filter
    - Future `since_iso` → empty list
    - Non-existent session → empty list
    - Invalid `since_iso` → `ValueError`
    - Result is oldest-first
  - `tests/test_tools/test_executor.py`:
    - Handler without `agent_config` param: kwargs unchanged (existing behavior preserved)
    - Handler with `agent_config: AgentConfig` param: correct `agent_config` injected

**Why second:** Both changes are infra that 42.3 depends on. Keeping them in one PR means
42.3 can open against a clean, tested foundation.

**Modified files:**
- `openrattler/storage/transcripts.py`
- `openrattler/tools/executor.py`
- `tests/test_storage/test_transcripts.py`
- `tests/test_tools/test_executor.py`

**Done when:** `pytest`, `mypy`, `black` pass. Existing transcript and executor test suites
remain green.

---

### Piece 42.3 — session_read_self + session_lookup tools

**Build:**
- `openrattler/tools/builtin/session_tools.py` — add alongside existing `sessions_history`:
  - Module-level: `_summarizer_agent: Optional[SummarizerAgent] = None`
  - Add `configure_summarizer_agent(agent: Optional[SummarizerAgent]) -> None`

  - `session_read_self` tool:
    - `trust_level_required=TrustLevel.main`, `action_level=5`
    - Params (LLM-visible): `since_iso: str`, `max_turns: int = 20`,
      `summarize: bool = True`
    - Runtime-injected (LLM-invisible): `agent_config: AgentConfig`
    - Logic:
      1. `session_key = agent_config.session_key` — reads CALLER's own session
      2. `TranscriptStore.load_since(session_key, since_iso, max_turns)`
      3. Audit `session_read_own` with turn count
      4. If `summarize=True`: serialize messages → SummarizerAgent with `skill="session"`
      5. Return `{"session_key", "turn_count", "since_iso", "summary"/"messages", "summarized"}`

  - `session_lookup` tool:
    - `trust_level_required=TrustLevel.main`, `action_level=3`
    - Params (LLM-visible): `session_key: str`, `since_iso: str`,
      `max_turns: int = 20`, `summarize: bool = True`
    - Runtime-injected: `agent_config: AgentConfig`
    - Handler security (after gate, defense-in-depth):
      ```python
      caller_key = (agent_config.session_key or "")
      is_calibration = caller_key.startswith("agent:main:heartbeat:")
      if is_calibration and session_key != "agent:main:main":
          audit("session_lookup_scope_violation", ...)
          return {"error": "CalibrationAgent may only read agent:main:main"}
      if is_calibration:
          since_dt = parse(since_iso)
          if since_dt < utcnow() - timedelta(hours=72):
              audit("session_lookup_scope_violation", ...)
              return {"error": "CalibrationAgent time window exceeded (max 72h)"}
      ```
    - `TranscriptStore.load_since(session_key, since_iso, max_turns)`
    - Audit `session_lookup_read`: requester session_key, target session_key,
      since_iso, turn_count returned
    - If `summarize=True`: same SummarizerAgent path
    - Returns same shape as `session_read_self`

  - Keep `sessions_history` — mark docstring `[DEPRECATED: use session_read_self or
    session_lookup]`, no behavior change

- Startup wiring (wherever executor `heartbeat_bypass_ops` is configured — likely
  `openrattler/runtime/startup.py` or `openrattler/cli/run.py`):
  - Add `"session_lookup"` to the heartbeat bypass ops set so CalibrationAgent's
    sessions (`agent:main:heartbeat:*`) skip the approval gate
  - Add `configure_summarizer_agent(summarizer)` call alongside existing
    `configure_transcript_store(store)` call

- Tests in `tests/test_tools/test_session_tools.py` (additions):
  - `session_read_self` uses `agent_config.session_key` (not a caller-supplied key)
  - `session_read_self(summarize=False)` returns raw messages list
  - `session_read_self(summarize=True)` returns summary dict from SummarizerAgent
  - `session_lookup` with CalibrationAgent caller + `session_key="agent:main:main"` → succeeds
  - `session_lookup` with CalibrationAgent caller + other session_key → scope violation error
  - `session_lookup` with CalibrationAgent caller + `since_iso` older than 72h → error
  - `session_lookup` audit entry contains requester, target, since_iso, turn_count
  - `sessions_history` still works (regression guard)

**Why last:** Depends on 42.1 (SummarizerAgent) and 42.2 (load_since + injection).

**Modified files:**
- `openrattler/tools/builtin/session_tools.py`
- `openrattler/runtime/startup.py` (or equivalent startup file)
- `tests/test_tools/test_session_tools.py`

**Done when:** `pytest`, `mypy`, `black` pass. Security scoping tests pass. Audit entries
verified to contain all required fields. `sessions_history` regression test green.

---

## Edge Cases & Open Questions

**42.1:**
- ⚠ Strip `"anthropic/"` model prefix before provider call (same as ResearchAgent pattern)
- ⚠ `named_skills_dir=None`: must fall back to `skill_prompt_path` silently — no exception
- ? 100k char input may be large for haiku's effective context — monitor in practice

**42.2:**
- ⚠ Timezone normalization: `UniversalMessage.timestamp` has tzinfo; caller-supplied
  `since_iso` may be naive — convert both to UTC before `>=` comparison
- ⚠ `load_since` reads the whole file before filtering — acceptable for typical session
  sizes; document as a known optimization opportunity if very long sessions appear

**42.3:**
- ⚠ `agent_config.session_key` is `Optional[str]` — null-guard:
  `(agent_config.session_key or "").startswith("agent:main:heartbeat:")`
- ⚠ If `"session_lookup"` is not added to heartbeat bypass ops, CalibrationAgent calls
  will block indefinitely at the approval gate — verify this in the startup wiring test
- ⚠ `session_read_self` with no matching turns (empty result) must return
  `{"turn_count": 0, ...}` not an error, so the caller can distinguish "nothing happened
  in this window" from a failure

---

## Status

Current milestone: **42.3 — complete**
Piece 42.1: ✅ complete — merged
Piece 42.2: ✅ complete — merged
Piece 42.3: ✅ complete — PR open
Last updated: 2026-05-19

---

---

# Build 44: Memory Security Audit Trail

Planned: 2026-05-22
References: Build 2.3 (Audit Log), Build 13.1 (Memory Security Agent)

## Status

Current milestone: **44.1 — complete**
Piece 44.1: ✅ complete — PR open
Last updated: 2026-05-22

---

---

# Build Plan: Token Economy Instrumentation (45.x)

Generated by /planner on 2026-05-29
Follows: Builds 39.x–44.x (all complete or in-flight)

---

## The Problem: Why Slack and Email Sessions Are Expensive

Before designing a solution, it's worth understanding exactly why Paul's 24-hour runs showed cheap heartbeat and research costs but expensive Slack/email interactions. The answer is in how session keys and transcript loading work.

**Heartbeat** creates a brand-new session key every cycle: `agent:main:heartbeat:hb_{cycle_id}`. When `AgentRuntime.initialize_session()` is called, it loads the transcript for that key — but the key is unique, so the transcript is always empty. Every heartbeat turn is effectively a **1-turn fresh context**. Cheap.

**Research agent** is ephemeral and calls `provider.complete()` directly, bypassing `AgentRuntime` entirely. No transcript accumulation possible. Cheap.

**Slack and email sessions** use a persistent key derived from the sender's hash: `agent:main:slack:{hash}`. `TranscriptStore.load()` is called on that key at the start of every session, returning **the full conversation history going back to the first message ever sent from that Slack user**. Turn 1 pays for system prompt + turn 1 context. Turn 2 pays for system prompt + turn 1 + turn 2 context. Turn 12 in a 24-hour session pays for system prompt + all 11 prior turns. This is the **context accumulation bomb**: prompt tokens grow roughly linearly with turns, making each successive turn progressively more expensive.

The instrumentation we're building will surface this pattern clearly. The primary metric to watch is **prompt tokens per turn over time within a session**. If that number is growing rapidly, you have context accumulation.

---

## What We're Building

Five pieces that together deliver a complete token economy instrumentation layer:

1. **`TurnRecord` model + `UsageStore`** — the storage foundation: one record per agent runtime turn, written to a dedicated JSONL file.
2. **`AgentRuntime` integration** — accumulate token usage across all LLM calls within a turn (the initial call plus every tool loop iteration), record a `TurnRecord` after each turn.
3. **Research agent token capture** — extend `ResearchAgent` to record its own direct `provider.complete()` calls as a synthetic session linked to the parent main-agent session via `trace_id`.
4. **Report generator** — read `TurnRecord`s from the store, group by session, compute session summaries, detect context accumulation, build a human-readable email report.
5. **Email delivery + scheduling** — send the report at server shutdown and on a configurable daily schedule.

---

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│  TOKEN TRACKING LAYER                                                │
│                                                                      │
│  AgentRuntime.process_message()                                      │
│    ├── Initial provider.complete()  ──► usage.prompt_tokens: 4,200  │
│    ├── Tool call: web_search                                         │
│    ├── Tool call: memory_read                                        │
│    ├── Loop provider.complete()    ──► usage.prompt_tokens: 5,100   │
│    │                                                                 │
│    │  Accumulated across ALL provider.complete() calls in one turn: │
│    │    prompt_tokens = 9,300                                        │
│    │    completion_tokens = 450                                      │
│    │    estimated_cost = $0.047                                      │
│    │    tool_calls = ["web_search", "memory_read"]                  │
│    │                                                                 │
│    └── Record TurnRecord ────────────────────────────────────────►  │
│                                                                      │
│  ResearchAgent._plan_search() + ._synthesize()                      │
│    ├── provider.complete() ──► usage accumulated                    │
│    └── Record TurnRecord (synthetic session key from trace_id) ──►  │
│                                                                      │
│            ▼                                                         │
│  UsageStore (append-only JSONL)                                      │
│  ~/.openrattler/usage/usage_log.jsonl                                │
│  one TurnRecord per line                                             │
│            │                                                         │
│            ▼                                                         │
│  UsageReportGenerator                                                │
│    ├── Load all TurnRecords in time window                           │
│    ├── Group by session_key                                          │
│    ├── Compute per-session summaries                                 │
│    ├── Detect context accumulation (prompt_tokens growth trend)      │
│    ├── Build channel/agent breakdown                                 │
│    └── Format HTML+text email report                                 │
│            │                                                         │
│            ▼                                                         │
│  Email delivery via existing EmailAdapter infrastructure             │
│  → paularthurmiller@gmail.com                                        │
│  Triggered at: server shutdown + configurable daily schedule         │
└─────────────────────────────────────────────────────────────────────┘
```

### The session_key → channel mapping

The session key format encodes agent type and channel. A simple parse table:

| Session key pattern            | agent_type    | channel     |
|-------------------------------|---------------|-------------|
| `agent:main:heartbeat:*`       | `heartbeat`   | `heartbeat` |
| `agent:main:slack:*`           | `main`        | `slack`     |
| `agent:main:email:*`           | `main`        | `email`     |
| `agent:main:main`              | `main`        | `cli`       |
| `agent:research:*`             | `research`    | `research`  |
| `agent:summarizer:*`           | `summarizer`  | `summarizer`|
| anything else                  | `unknown`     | `unknown`   |

### The subagent hierarchy

When the main agent calls `research_query`, the research agent runs in its own context. To tie the two together in the report, we link via `trace_id`:

```
Main session: agent:main:slack:{hash}
  Turn 3: prompt_tokens=12,000  tools=["research_query"]  trace_id=abc123
    └── Research session: agent:research:abc123
          Turn 1: prompt_tokens=1,200  model=claude-haiku  trace_id=abc123
```

The research session's `parent_session_key` field points to the main session. The report groups them together under the parent.

---

## Data Model

```
TurnRecord:
  recorded_at:          datetime    # when this record was written (UTC)
  session_key:          str         # full session key, e.g. "agent:main:slack:abc123"
  agent_type:           str         # "main" | "heartbeat" | "research" | "summarizer"
  channel:              str         # "slack" | "email" | "heartbeat" | "cli" | "research"
  parent_session_key:   Optional[str]  # for subagents: the session that spawned this
  trace_id:             Optional[str]  # links to parent message chain
  turn_number:          int         # 1, 2, 3 ... sequential within this session
  model:                str         # e.g. "claude-sonnet-4-6"
  prompt_tokens:        int         # total across ALL LLM calls in this turn
  completion_tokens:    int         # total across ALL LLM calls in this turn
  total_tokens:         int         # prompt + completion
  estimated_cost_usd:   float       # sum across all LLM calls in this turn
  llm_calls:            int         # how many provider.complete() calls in this turn
  tool_calls:           list[str]   # tool names invoked (may repeat if called twice)
  tool_loops:           int         # how many tool loop iterations
```

`turn_number` is derived at write time by counting prior `TurnRecord`s for the same `session_key`. The `UsageStore` handles this internally so callers don't need to track it.

---

## Data Flow: A Turn Through the System

```
USER (Slack): "Can you check my calendar for tomorrow?"
                  │
                  ▼
 SlackAdapter.receive() → UniversalMessage
   session_key = "agent:main:slack:d4f9a3"
   channel = "slack"
   trace_id = "uuid-abc123"
                  │
                  ▼
 AgentRuntime.process_message(session, user_message)
   accumulated_prompt_tokens = 0
   accumulated_completion_tokens = 0
   llm_calls = 0
                  │
                  ▼
 provider.complete(messages)  ← system_prompt + 3 prior turns + "Check calendar..."
   usage.prompt_tokens = 12,400   ← EXPENSIVE: full history loaded
   usage.completion_tokens = 120
   usage.model = "claude-sonnet-4-6"
   [tool_call: calendar_list_events]
                  │
                  ▼
 calendar_list_events() → executed, result added to messages
                  │
                  ▼
 provider.complete(messages + tool results)
   usage.prompt_tokens = 12,900   ← slightly larger (added tool result)
   usage.completion_tokens = 280
                  │
                  ▼
 Turn complete. Accumulated:
   prompt_tokens = 25,300   (sum of both calls)
   completion_tokens = 400
   total_tokens = 25,700
   estimated_cost_usd = $0.082
   tool_calls = ["calendar_list_events"]
   tool_loops = 1
   llm_calls = 2
                  │
                  ▼
 UsageStore.record_turn(TurnRecord(
   session_key = "agent:main:slack:d4f9a3",
   agent_type  = "main",
   channel     = "slack",
   turn_number = 4,           ← 4th turn in this session
   model       = "claude-sonnet-4-6",
   prompt_tokens = 25,300,    ← was 7,200 in turn 1: context is GROWING
   ...
 ))
```

The report generator sees prompt tokens go: 7,200 → 11,400 → 18,500 → 25,300 — a 3.5× growth over 4 turns. That's the context accumulation pattern, clearly surfaced.

---

## Report Format

```
╔══════════════════════════════════════════════════════════════════════════╗
║           OPENRATTLER TOKEN ECONOMY REPORT                             ║
║           2026-05-28 08:00 UTC → 2026-05-29 08:00 UTC (24h)          ║
╚══════════════════════════════════════════════════════════════════════════╝

SUMMARY
────────────────────────────────────────────────────
  Total estimated cost:   $3.84
  Total tokens:           1,847,320
  Active sessions:        2
  Closed sessions:        18

COST BY CHANNEL (24h)
────────────────────────────────────────────────────
  slack      $2.91  76%  ████████████████████
  email      $0.61  16%  ████
  heartbeat  $0.19   5%  █
  cli        $0.13   3%

COST BY AGENT TYPE (24h)
────────────────────────────────────────────────────
  main         $2.84  74%
  research     $0.71  18%
  heartbeat    $0.19   5%
  summarizer   $0.10   3%

─────────────────────────────────────────────────────
ACTIVE SESSIONS (still open — no close in last 60 min)
─────────────────────────────────────────────────────

  agent:main:slack:d4f9a3 | Channel: slack | Model: claude-sonnet-4-6
  Started: 2026-05-28 09:12 UTC | Last active: 2026-05-29 07:51 UTC (~8 min ago)
  Turns: 14 | Total tokens: 287,400 | Cost: $1.24
  Tools: calendar_list_events×4, memory_read×8, send_email×2, research_query×1

  ⚠ CONTEXT ACCUMULATION DETECTED
    Prompt tokens per turn: 4.2K → 9.8K → 16.1K → 24.3K → 33.7K ... → 82.1K
    Growth: 1,852% over 14 turns. This session is accumulating context rapidly.
    Consider: session timeout, context summarization, or manual session reset.

  └─ Sub-session: research (trace_id=abc...) | 2026-05-28 14:22 UTC
       Turns: 1 | Tokens: 18,400 | Cost: $0.038 | Model: claude-haiku-4-5
       Tools: web_search×1, web_fetch×3

────────────────────────────────────────────────────
CLOSED SESSIONS (last 24h, most recent first)
────────────────────────────────────────────────────

  [heartbeat] 2026-05-29 07:00 UTC
    Turns: 1 | Tokens: 5,200 | Cost: $0.007 | Model: claude-haiku-4-5
    Tools: send_email×1, update_memory_narrative×1

  [heartbeat] 2026-05-29 06:00 UTC
    Turns: 1 | Tokens: 4,800 | Cost: $0.006 | Model: claude-haiku-4-5
    Tools: (none this cycle)

  [slack] agent:main:slack:d4f9a3 | Closed 2026-05-28 09:11 UTC (idle timeout)
    Duration: 2h 34m | Turns: 5 | Total tokens: 42,300 | Cost: $0.36
    Prompt tokens per turn: 3.1K → 7.8K → 14.2K → 22.1K → 28.9K
    ⚠ Moderate context growth (830% over 5 turns)

  ...

─────────────────────────────────────────────────────
CONTEXT ACCUMULATION WARNINGS (sessions with >500% growth)
─────────────────────────────────────────────────────

  1. agent:main:slack:d4f9a3 (active)
     Prompt tokens: 4.2K → 82.1K | Growth: 1,852% | 14 turns | Cost: $1.24

  2. agent:main:email:b7c2e1 (closed 2026-05-28 22:14 UTC)
     Prompt tokens: 3.8K → 41.2K | Growth: 984% | 8 turns | Cost: $0.68

─────────────────────────────────────────────────────
GENERATED: 2026-05-29 08:00:12 UTC | OpenRattler v1.0
```

---

## Key Files

```
openrattler/
├── models/
│   └── usage.py                   ← NEW: TurnRecord model
├── storage/
│   └── usage.py                   ← NEW: UsageStore (JSONL, append-only)
├── agents/
│   ├── runtime.py                 ← MODIFIED: inject UsageStore, accumulate + record per turn
│   └── research/
│       └── agent.py               ← MODIFIED: inject UsageStore, record research turns
├── reports/
│   ├── __init__.py                ← NEW: reports package
│   └── usage_report.py            ← NEW: UsageReportGenerator
└── startup.py                     ← MODIFIED: wire UsageStore + report scheduling

~/.openrattler/usage/
└── usage_log.jsonl                ← runtime data: one TurnRecord per line
```

### Why a separate `reports/` package?

Report generation is distinct from storage (reads records, produces formatted text) and distinct from delivery (email/SMS). Keeping it in its own package means:
- `storage/usage.py` is a pure append-and-read store (no formatting logic)
- `reports/usage_report.py` is pure data processing (no I/O beyond reading the store)
- `startup.py` wires everything together for delivery

This is the same separation principle as `tools/builtin/` (tool surface) vs `integrations/google/` (API client).

---

## Build Order

---

### Piece 45.1 — `TurnRecord` Model + `UsageStore`

**Build:**

Create `openrattler/models/usage.py`:
```python
class TurnRecord(BaseModel):
    recorded_at: datetime           # UTC, set at write time
    session_key: str
    agent_type: str                 # "main", "heartbeat", "research", "summarizer", "unknown"
    channel: str                    # "slack", "email", "heartbeat", "cli", "research", "unknown"
    parent_session_key: Optional[str] = None
    trace_id: Optional[str] = None
    turn_number: int                # 1-indexed, assigned by UsageStore
    model: str
    prompt_tokens: int
    completion_tokens: int
    total_tokens: int
    estimated_cost_usd: float
    llm_calls: int                  # how many provider.complete() calls in this turn
    tool_calls: list[str] = Field(default_factory=list)
    tool_loops: int = 0
```

Add a module-level helper:
```python
def infer_channel_and_agent(session_key: str) -> tuple[str, str]:
    """Return (channel, agent_type) from session_key.

    Parses the session_key format:
      agent:main:heartbeat:* → ("heartbeat", "heartbeat")
      agent:main:slack:*     → ("slack", "main")
      agent:main:email:*     → ("email", "main")
      agent:main:main        → ("cli", "main")
      agent:research:*       → ("research", "research")
      agent:summarizer:*     → ("summarizer", "summarizer")
      everything else        → ("unknown", "unknown")
    """
```

Create `openrattler/storage/usage.py` — `UsageStore`:
- `__init__(log_path: Path)` — path to `usage_log.jsonl`; parent created on first write
- `async record_turn(record_data: dict[str, Any]) -> None`:
  - Look up how many prior records exist for `session_key` → derive `turn_number`
  - Set `recorded_at = datetime.now(timezone.utc)`
  - Append as JSONL line; same `asyncio.to_thread` + per-session lock pattern as `TranscriptStore`
- `async load_since(since: datetime, max_records: int = 10_000) -> list[TurnRecord]`:
  - Read all lines, filter by `recorded_at >= since`, cap to `max_records`
  - Returns empty list if file does not exist
- `async count_for_session(session_key: str) -> int`:
  - Count existing records for this session (used internally for `turn_number` assignment)

Note: `count_for_session` reads the whole file, which is fine for typical log sizes (thousands of records). An in-memory counter could be added later if the file grows large.

**Tests** in `tests/test_storage/test_usage_store.py`:
- `record_turn` writes a parseable JSONL line
- `turn_number` increments correctly for the same session (1 → 2 → 3)
- `turn_number` is independent per session (two different sessions each start at 1)
- `load_since` filters correctly (past cutoff → excluded, at/after cutoff → included)
- `load_since` caps to `max_records`
- Non-existent file → empty list, no crash
- `infer_channel_and_agent`: all patterns from the table return correct tuples
- `infer_channel_and_agent`: unknown prefix → ("unknown", "unknown")

**Why first:** Zero dependencies on any other 45.x piece. Pure model + I/O. All other pieces depend on these two files.

**New files:**
- `openrattler/models/usage.py`
- `openrattler/storage/usage.py`
- `tests/test_storage/test_usage_store.py`

**Done when:** `pytest`, `mypy`, `black` pass. `turn_number` assignment correct. JSONL round-trip verified.

---

### Piece 45.2 — `AgentRuntime` Token Accumulation

**Build:**

Modify `openrattler/agents/runtime.py`:

Add constructor parameter:
```python
def __init__(
    self,
    ...
    usage_store: Optional["UsageStore"] = None,
) -> None:
    ...
    self._usage_store = usage_store
```

Modify `process_message()` to accumulate token usage across all LLM calls in a turn:

```python
async def process_message(self, session, user_message) -> UniversalMessage:
    # ... existing setup ...
    
    # Usage accumulators for this turn
    _prompt_tokens = 0
    _completion_tokens = 0
    _cost_usd = 0.0
    _model = ""
    _llm_calls = 0
    _all_tool_names: list[str] = []

    try:
        # 3. Initial LLM call
        last_response = await self._provider.complete(...)
        _prompt_tokens += last_response.usage.prompt_tokens
        _completion_tokens += last_response.usage.completion_tokens
        _cost_usd += last_response.usage.estimated_cost_usd
        _model = last_response.model
        _llm_calls += 1

        # 4. Tool loop
        while last_response.tool_calls and tool_loop_count < _MAX_TOOL_LOOPS:
            tool_loop_count += 1
            for tc in last_response.tool_calls:
                _all_tool_names.append(tc.tool_name)   # collect before executing
                # ... existing execution ...
            
            last_response = await self._provider.complete(...)
            _prompt_tokens += last_response.usage.prompt_tokens
            _completion_tokens += last_response.usage.completion_tokens
            _cost_usd += last_response.usage.estimated_cost_usd
            _model = last_response.model
            _llm_calls += 1
        
        # ... existing message building, audit log ...

    except Exception as exc:
        # ... existing error handling ...

    # Record usage (after audit log, non-blocking — failure does not affect response)
    if self._usage_store is not None:
        try:
            channel, agent_type = infer_channel_and_agent(session_key)
            await self._usage_store.record_turn({
                "session_key": session_key,
                "agent_type": agent_type,
                "channel": channel,
                "trace_id": user_message.trace_id,
                "model": _model,
                "prompt_tokens": _prompt_tokens,
                "completion_tokens": _completion_tokens,
                "total_tokens": _prompt_tokens + _completion_tokens,
                "estimated_cost_usd": _cost_usd,
                "llm_calls": _llm_calls,
                "tool_calls": _all_tool_names,
                "tool_loops": tool_loop_count,
            })
        except Exception as usage_exc:
            logger.warning("AgentRuntime: failed to record usage: %s", usage_exc)

    return assistant_msg
```

Critical design decisions:
- Usage recording is **after** the response is built and audit-logged — it never blocks or delays the user's response
- Failure to record usage is caught and logged as a WARNING, not re-raised — token tracking is never mission-critical
- `_all_tool_names` is built from tool calls **before** execution (we want to know what was requested, not just what succeeded)
- The accumulator pattern correctly handles 0-tool-loop turns (just the initial call) and multi-loop turns

Modify `openrattler/startup.py`:
- After `UsageStore` is constructed (new step 3b, between AuditLog and TranscriptStore):
  ```python
  from openrattler.storage.usage import UsageStore
  usage_store = UsageStore(workspace_dir / "usage" / "usage_log.jsonl")
  ```
- Pass `usage_store=usage_store` to `AgentRuntime(...)` constructor call

**Tests** in `tests/test_agents/test_runtime.py` (additions):
- Turn with no tools: one TurnRecord written with `llm_calls=1`, `tool_calls=[]`
- Turn with two tool calls: one TurnRecord with `tool_calls=["tool_a", "tool_b"]`
- Multi-loop turn: accumulated `prompt_tokens` is sum across all LLM calls in the loop
- `usage_store=None`: no record written, no error
- Usage store failure (mock to raise): response returned normally, warning logged
- `turn_number` increments correctly across two `process_message()` calls on the same session

**Note on tool_calls ordering:** Tool names are collected in the order they appear in `last_response.tool_calls` at each loop iteration. If the same tool is called twice in two separate loops, it appears twice in the list. This is the right behavior — the report can count occurrences.

**Why second:** Depends on 45.1 for `UsageStore`. All other pieces depend on this one being wired.

**Modified files:**
- `openrattler/agents/runtime.py`
- `openrattler/startup.py`
- `tests/test_agents/test_runtime.py`

**Done when:** `pytest`, `mypy`, `black` pass. A real CLI session produces records in `usage_log.jsonl`. Each turn's accumulated tokens match the sum of all provider.complete() calls in that turn.

---

### Piece 45.3 — Research Agent Token Capture

**Build:**

Modify `openrattler/agents/research/agent.py`:

Add optional `UsageStore` parameter to `__init__`:
```python
def __init__(
    self,
    config: ResearchAgentConfig,
    audit: AuditLog,
    provider: Optional[LLMProvider] = None,
    usage_store: Optional["UsageStore"] = None,
    parent_session_key: Optional[str] = None,
) -> None:
    ...
    self._usage_store = usage_store
    self._parent_session_key = parent_session_key
```

Derive a synthetic session key for the research session. This happens in `run()`:
```python
# Inside run(), after receiving the request
research_session_key = f"agent:research:{(trace_id or 'no-trace')[:12]}"
```

Accumulate usage across `_plan_search` and `_synthesize` calls. Add private tracking vars:
```python
self._turn_prompt_tokens: int = 0
self._turn_completion_tokens: int = 0
self._turn_cost_usd: float = 0.0
self._turn_model: str = ""
self._turn_llm_calls: int = 0
```

Reset these at the start of each `run()` call. Update them after each `provider.complete()` call in `_plan_search` and `_synthesize`:
```python
# In _plan_search, after response = await self._provider.complete(...):
self._turn_prompt_tokens += response.usage.prompt_tokens
self._turn_completion_tokens += response.usage.completion_tokens
self._turn_cost_usd += response.usage.estimated_cost_usd
self._turn_model = response.model
self._turn_llm_calls += 1
```

At the end of `run()`, before returning the UM:
```python
if self._usage_store is not None and self._turn_llm_calls > 0:
    try:
        await self._usage_store.record_turn({
            "session_key": research_session_key,
            "agent_type": "research",
            "channel": "research",
            "parent_session_key": self._parent_session_key,
            "trace_id": trace_id,
            "model": self._turn_model,
            "prompt_tokens": self._turn_prompt_tokens,
            "completion_tokens": self._turn_completion_tokens,
            "total_tokens": self._turn_prompt_tokens + self._turn_completion_tokens,
            "estimated_cost_usd": self._turn_cost_usd,
            "llm_calls": self._turn_llm_calls,
            "tool_calls": [],    # research agent uses web_search/web_fetch but not via AgentRuntime tool loop
            "tool_loops": 0,
        })
    except Exception as usage_exc:
        logger.warning("ResearchAgent: failed to record usage: %s", usage_exc)
```

Wire `usage_store` and `parent_session_key` through `AgentCreator.create_research_agent()`:
- `ResearchTools.handle()` has access to the tool call's `session_key` (from `agent_config.session_key`)
- Pass it as `parent_session_key` when creating the research agent
- Pass the shared `usage_store` instance

Modify `openrattler/tools/builtin/research_tools.py`:
- Add `_usage_store: Optional[UsageStore] = None` module-level variable
- Add `configure_usage_store(store: Optional[UsageStore]) -> None` function
- In `handle()`, pass `usage_store=_usage_store` and `parent_session_key=agent_config.session_key` to `AgentCreator.create_research_agent()`

Modify `openrattler/startup.py`:
- After creating `usage_store`, call `configure_usage_store(usage_store)` alongside existing `configure_transcript_store()` and similar calls

**Tests** in `tests/test_agents/test_research/test_agent.py` (additions):
- `usage_store=None`: no record written, no crash
- `usage_store` provided + provider makes 2 calls: `llm_calls=2`, accumulated tokens correct
- `parent_session_key` set correctly on the written record
- `research_session_key` derived from `trace_id`
- Usage recording failure: research result still returned normally

**Why third:** Depends on 45.1 (UsageStore). Research agent's LLM call pattern (direct provider calls, not through AgentRuntime) requires a dedicated hook that 45.2 doesn't cover.

**Modified files:**
- `openrattler/agents/research/agent.py`
- `openrattler/tools/builtin/research_tools.py`
- `openrattler/agents/creator.py` (if `create_research_agent` needs additional params)
- `openrattler/startup.py`
- `tests/test_agents/test_research/test_agent.py`

**Done when:** `pytest`, `mypy`, `black` pass. A real heartbeat that triggers research writes both a main agent `TurnRecord` (from 45.2) and a research `TurnRecord` (from 45.3), linked via `trace_id`.

---

### Piece 45.4 — Report Generator

**Build:**

Create `openrattler/reports/__init__.py`.

Create `openrattler/reports/usage_report.py` — `UsageReportGenerator`:

```python
class SessionSummary:
    """Aggregated view of all TurnRecords for one session."""
    session_key: str
    agent_type: str
    channel: str
    parent_session_key: Optional[str]
    first_turn_at: datetime
    last_turn_at: datetime
    turns: int
    total_prompt_tokens: int
    total_completion_tokens: int
    total_tokens: int
    total_cost_usd: float
    models_used: list[str]           # deduplicated list
    tool_calls: dict[str, int]       # {tool_name: count}
    prompt_tokens_per_turn: list[int]  # for context growth analysis
    is_active: bool                  # last_turn_at > now - idle_timeout_minutes


class UsageReportGenerator:
    def __init__(
        self,
        usage_store: UsageStore,
        idle_timeout_minutes: int = 60,
    ) -> None: ...

    async def generate(
        self,
        since: Optional[datetime] = None,  # None → last 24 hours
        until: Optional[datetime] = None,  # None → now
    ) -> str:
        """Generate a plain-text email report for the given time window."""
```

`generate()` pipeline:
1. `load_since(since)` → list of all `TurnRecord`s in window
2. Group records by `session_key` → dict of `SessionSummary` objects
3. Sort sessions: active sessions first, then closed sessions by `last_turn_at` descending
4. Link research sessions to parent sessions via `parent_session_key`
5. Compute report-level aggregates (total cost, tokens, channel breakdown, agent breakdown)
6. Detect context accumulation: for each session, compute `prompt_tokens_per_turn` list; flag sessions where the last turn's prompt_tokens is >500% of the first turn's
7. Format and return plain-text report (see Report Format section above)

`_context_growth_pct(per_turn: list[int]) -> Optional[float]`:
- Returns `(last - first) / first * 100` if len >= 2, else None

`_format_bar(pct: float, width: int = 20) -> str`:
- Returns a simple ASCII bar like `████████████░░░░░░░░` representing the percentage

`_format_tool_summary(tool_calls: dict[str, int]) -> str`:
- Returns `send_email×2, memory_read×5, research_query×1` style summary

`_is_active(last_turn_at: datetime, idle_timeout_minutes: int) -> bool`:
- Returns `True` if `now - last_turn_at < timedelta(minutes=idle_timeout_minutes)`

**Tests** in `tests/test_reports/test_usage_report.py`:
- Empty store → report says "No sessions in this window" (no crash)
- Single heartbeat turn → shows in closed sessions with correct tokens/cost
- Multi-turn Slack session → context growth computed correctly; ⚠ flag when > 500%
- Research session linked to parent → shows as sub-session under parent
- Active vs closed classification based on `idle_timeout_minutes`
- Tool call counts: same tool called twice → `tool_name×2`
- Report contains all required sections: SUMMARY, COST BY CHANNEL, COST BY AGENT TYPE,
  ACTIVE SESSIONS, CLOSED SESSIONS, CONTEXT ACCUMULATION WARNINGS
- Channel breakdown percentages sum to 100%

**Why fourth:** Pure data processing — reads from the store (45.1), produces formatted output. No dependency on 45.2 or 45.3; it works with whatever TurnRecords are in the store. Can be tested entirely with mock TurnRecords written directly to the store.

**New files:**
- `openrattler/reports/__init__.py`
- `openrattler/reports/usage_report.py`
- `tests/test_reports/__init__.py`
- `tests/test_reports/test_usage_report.py`

**Done when:** `pytest`, `mypy`, `black` pass. A report generated from a test store with varied sessions (heartbeat, slack multi-turn, research sub-session) produces correct section headers, accurate numbers, and correctly flags context accumulation.

---

### Piece 45.5 — Email Delivery + Scheduling

**Build:**

Add `UsageReportConfig` to `openrattler/config/loader.py`:
```python
class UsageReportConfig(BaseModel):
    enabled: bool = True
    send_on_shutdown: bool = True       # send a report when server stops cleanly
    scheduled_hour_utc: int = 8         # daily digest at 08:00 UTC (0-23)
    idle_session_timeout_minutes: int = 60  # sessions with no activity for this long are "closed"
    report_window_hours: int = 24       # how far back the report looks
    recipient_email: Optional[str] = None  # None → uses default_to_address from email channel
```

Add `usage_report: UsageReportConfig` to `AppConfig`.

Create `openrattler/reports/email_delivery.py` — `UsageReportDelivery`:
```python
class UsageReportDelivery:
    """Generates and emails a usage report."""

    def __init__(
        self,
        generator: UsageReportGenerator,
        email_config: ChannelConfig,
        report_config: UsageReportConfig,
    ) -> None: ...

    async def send_report(self, since: Optional[datetime] = None) -> bool:
        """Generate and email the usage report. Returns True on success."""
        report_text = await self._generator.generate(since=since)
        # Build and send via the existing email infrastructure
        # Subject: "OpenRattler Token Report — {date}"
        # Body: plain-text report
        # Uses smtplib (same approach as EmailAdapter.send())
        ...
```

Modify `openrattler/startup.py`:

Step 3b (after AuditLog, before TranscriptStore) — create UsageStore:
```python
usage_store = UsageStore(workspace_dir / "usage" / "usage_log.jsonl")
```

Step 13.5 (after scheduler is built, before gateway) — wire report delivery:
```python
if config.usage_report.enabled and email_channel_is_configured:
    report_generator = UsageReportGenerator(
        usage_store=usage_store,
        idle_timeout_minutes=config.usage_report.idle_session_timeout_minutes,
    )
    report_delivery = UsageReportDelivery(
        generator=report_generator,
        email_config=email_channel_config,
        report_config=config.usage_report,
    )
    # Register daily scheduled report via the ProcessorScheduler
    # Triggered at config.usage_report.scheduled_hour_utc each day
```

Add shutdown hook in `ApplicationContext.shutdown()` (or equivalent):
```python
if config.usage_report.send_on_shutdown and report_delivery is not None:
    try:
        since = datetime.now(timezone.utc) - timedelta(
            hours=config.usage_report.report_window_hours
        )
        await report_delivery.send_report(since=since)
        logger.info("Usage report sent on shutdown")
    except Exception as exc:
        logger.warning("Failed to send usage report on shutdown: %s", exc)
```

For the **daily scheduled report**: hook into the existing `ProcessorScheduler` — add a lightweight `UsageReportProcessor` that implements `ProactiveProcessor.run_cycle()` and fires `report_delivery.send_report()` once per day at the configured hour. The scheduler already handles periodic processor runs.

**Tests** in `tests/test_reports/test_email_delivery.py`:
- Report generated successfully → `send_report()` returns True
- Email channel not configured → `send_report()` returns False, no crash
- Report generator raises → `send_report()` catches, returns False, logs warning
- Subject contains today's date
- Body is the formatted report text

**Tests** in `tests/test_config/test_loader.py` (additions):
- `UsageReportConfig` defaults: enabled=True, send_on_shutdown=True, scheduled_hour_utc=8
- `scheduled_hour_utc` validated in range 0-23

**Why last:** Depends on the generator (45.4) which depends on the store (45.1). The scheduling hook depends on the startup wiring from 45.2/45.3. This piece is the integration point that connects all the others.

**New files:**
- `openrattler/reports/email_delivery.py`
- `tests/test_reports/test_email_delivery.py`

**Modified files:**
- `openrattler/config/loader.py`
- `openrattler/startup.py`

**Done when:** `pytest`, `mypy`, `black` pass. Start the server, run a CLI chat session, stop the server cleanly → an email arrives with the session's token data. The daily scheduler fires at the configured hour.

---

## Edge Cases & Open Questions

**45.1:**
- ⚠ `count_for_session` reads the full file to assign `turn_number`. For a long-running server with thousands of records, this gets slower over time. Mitigation: in-memory counter cache (`dict[str, int]`) that persists across `record_turn` calls. Initialize by reading the file once on first access per session. Add this if the file exceeds ~10k records.
- ? Should `usage_log.jsonl` be rotated? Daily rotation (one file per day) would keep query windows fast but require multi-file reads in `load_since`. For now: single file, note this as a future optimization if the file grows large (>100k records = ~50MB). Not urgent for a personal assistant with moderate usage.
- ⚠ `turn_number` could drift if the usage_log.jsonl file is deleted or rotated mid-session. This is acceptable — the turn numbers are purely informational for reading trends, not used for any security or business logic.

**45.2:**
- ⚠ The `_all_tool_names` list is built from `last_response.tool_calls` BEFORE execution. If the tool executor fails the call (dead-stop, denial, etc.), the tool name still appears in the list. This is intentional — we want to know what the LLM *attempted*, not just what succeeded. The audit log records which executions were blocked.
- ? Should `usage_store` be passed to `CLIChat.open()` as well (for CLI sessions)? Yes — add it to 45.2 scope. `CLIChat` builds its own `AgentRuntime` separately from `startup.py`.
- ⚠ If the server is killed hard (SIGKILL, power loss) rather than clean shutdown, the in-flight turn's usage is lost. This is acceptable — the next turn's record will show an unusually low turn_number or a gap. Hard kills also prevent the shutdown report from sending.

**45.3:**
- ⚠ `AgentCreator.create_research_agent()` currently returns a `ResearchAgent`. Passing `usage_store` through requires adding it as a parameter to `create_research_agent`. Check whether `creator.py` imports `UsageStore` — use a `TYPE_CHECKING` guard to avoid circular imports.
- ? The `SummarizerAgent` also calls `provider.complete()` directly. Track it in 45.3 scope? Decision: yes, add `usage_store` to `SummarizerAgent` using the same pattern as `ResearchAgent`. The session key: `agent:summarizer:{trace_id[:12]}`.
- ⚠ `parent_session_key` in research turns is derived from `agent_config.session_key` in `ResearchTools.handle()`. But `agent_config` is injected at executor level — confirm that `handle()` receives it (via the executor injection mechanism from 42.2).

**45.4:**
- ⚠ Sessions that span across the report window boundary: first turn at 2026-05-28 23:00, last turn at 2026-05-29 02:00 for a 24h window ending at 2026-05-29 08:00. Include partial sessions? Decision: include all sessions where ANY turn falls within the window. This matches user expectations (they want to see costs for all work done in the last 24h).
- ⚠ Context growth warning threshold (500%) is a starting point. After seeing real data, Paul may want to tune this. Make it configurable: `context_growth_warning_pct: float = 500.0` in `UsageReportConfig`.
- ? Sorting closed sessions: most recent first (last_turn_at descending) is the obvious choice. But Paul may prefer sorted by cost (highest first) to see what's most expensive. Add both? For v1: most recent first, with total cost shown. Paul can always request a "by cost" view as a future feature.

**45.5:**
- ⚠ The daily report scheduler needs to know when "08:00 UTC" falls relative to the current time to schedule the first run. Use `ProcessorScheduler`'s interval mechanism (already in place) with an interval of 24h. The exact "at 08:00 UTC" alignment is nice-to-have — if the server starts at 15:00 UTC, the first report fires 24h later at 15:00, not at 08:00 the next morning. Exact daily alignment can be added as a future enhancement; the 24h interval is sufficient for the primary use case.
- ⚠ Sending a usage report requires the email channel to be configured. If the email channel is not configured, skip silently (log at INFO, don't warn — it's not an error to not have email).
- ? Should the report also be logged to a file so Paul can read it without email? Add `report_log_path: Optional[str] = None` to `UsageReportConfig`. When set, write the report text to that path in addition to (or instead of) emailing. Not in v1 scope — email is sufficient.

---

## Conventions for This Build

- `UsageStore` follows the same file-per-type JSONL pattern as `AuditLog` and `TranscriptStore` — consistent, predictable, easy to inspect with `cat`.
- Usage recording never blocks the response path and never raises. Every `record_turn` call is wrapped in try/except at the call site.
- All token numbers are integers (from the API). Cost estimates are floats, displayed with 3 decimal places in the report.
- The `infer_channel_and_agent()` helper lives in `models/usage.py` (with the `TurnRecord` model) so the report generator doesn't need to import from `agents/runtime.py` to parse session keys.
- Research and summarizer synthetic session keys use the format `agent:{type}:{trace_id[:12]}` — this is long enough to be unique across concurrent sessions but short enough to be readable.
- `UsageReportConfig.enabled = False` completely disables all tracking (no records written, no delivery). Useful for tests and for explicitly opting out.

---

## Dependencies & Setup Prerequisites

- No new Python packages required. The report generator uses only stdlib (string formatting, datetime). Email delivery reuses the existing `smtplib` infrastructure.
- `UsageReportConfig.recipient_email` defaults to `None`, which means the report goes to `default_to_address` from the email channel config. If the email channel isn't configured, reports are skipped.
- `~/.openrattler/usage/` directory is created automatically on first write by `UsageStore`.

---

## Status

Current milestone: **45.5 — not started**
Piece 45.1: ✅ complete — PR pending
Piece 45.2: ✅ complete — PR pending
Piece 45.3: ✅ complete — PR pending
Piece 45.4: ✅ complete — PR pending
Piece 45.5: ⏳ not started (depends on 45.2, 45.3, 45.4)
Last updated: 2026-05-29
